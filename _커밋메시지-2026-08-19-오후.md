# 2026-08-19 오후 실행분 — 운영자에게

## ⚠ 먼저 하실 일 — 자동 푸시가 오전부터 멈춰 있습니다

`.auto-push.log` 가 **06:20 부터 10분마다 계속 빌드 실패**로 끊기고 있습니다.
원인은 오늘 오전에 이미 지시문 ⑥-A 에 기록된 그 사고입니다 — 윈도우 쪽
`node_modules/.bin` 폴더가 통째로 없고 `node_modules/astro` 는 껍데기만 남았습니다.
(샌드박스에서 확인: `.bin` 없음, `astro/` 안에 `components` 와 `node_modules` 만 있음)

그래서 **`npm run build` 가 `'astro' is not recognized` 를 냅니다.**

→ **`1-노드확인하고-푸시.bat` 을 한 번 돌려 주십시오.**
   그 파일은 AppCheck 때문에 node_modules 를 지우지 않고 이름만 바꾼 뒤 `npm ci` 를 합니다.

이것을 하기 전에는 **오늘 쓴 것이 하나도 올라가지 않습니다.**
`e021ed2 Decode 5편` 도 커밋만 되고 아직 푸시되지 못한 상태입니다.

잠금 파일(`.git/index.lock` · `HEAD.lock`)은 **없습니다.** 지우실 것 없습니다.

---

## 커밋 메시지 (그대로 쓰시면 됩니다)

```
Scenes 10편 — 한국영화에는 자막이 없다, 그리고 상암에 무료로 있는 예외

VERIFIED - astro build 통과 (112 pages)

■ 왜 이 축인가
  세어 보니 scenes 9 / hangul 5 / decode 5 / now 18 / guides 10 (총 47).
  목표(30/20/20/20/10) 대비 미달폭이 Scenes −5.1 로 가장 컸다. Hangul·Decode 는
  각 −4.4 이고 어제·오늘 이미 한 편씩 채웠다. Now 는 +8.6 으로 과잉이라 손대지 않았다.

  그리고 이 축에는 **K무비가 한 편도 없었다.** 홈이 K드라마·K팝·K무비·K공연을
  약속하는데 영화 기사는 0편이고, 「영화관」이라는 낱말이 47편 어디에도 없었다.

■ 무엇을 확인했나 (전부 1차 출처, 2026-08-19 열람)
  · 시네마테크KOFA 무료·화~토·일월 휴관·정시 상영·현장 예매 규칙 — 자료원 영문 안내면
  · 자막 범례 E = 영어자막 / 한국영화 외 전 상영작 한글자막 — 국문 상영일정면 + 영문 프로그램면
  · 「Citizen of a Kind [English Subtitles]」 — 제목에 자막 여부를 박아 넣는 표기 확인
  · 한국영화박물관 무료·화~토 10:30–19:00 — 자료원 영문 안내면
  · 주소 400 월드컵북로 마포구 03925 — 시설안내면

  ▸ 우리만 아는 것 ①  영화진흥위원회 KoBiz 의 영문 「Screenings with Eng. Subtitles」
    두 판이 각각 **2025-05-01~07** 과 **2019-12-15~21** 에 멈춰 있다.
    한국이 외국인에게 영어자막 상영을 안내하는 공식 창구가 15개월 밀려 있다는 뜻이고,
    이게 이 기사의 척추다. english-pages-korea-out-of-date 편과 곧장 이어진다.

  ▸ 우리만 아는 것 ②  자료원 영문 시설안내면이 상암 전화번호를 **+81** (일본 국가번호)로
    적어 두었다. 같은 사이트 푸터는 **+82** 다. 번호 자체는 같으므로 푸터가 맞고
    시설면이 오타다. 해외에서 그 페이지를 보고 걸면 다른 나라로 간다.

■ 확인하지 못한 것 (본문 맺음말에 전부 적었다)
  · **현재 상영일정을 못 읽었다.** 국문 상영일정·캘린더면이 브라우저에서 조립되는 구조라
    받은 것이 수년 전 렌더였다(같은 블록에 코로나 거리두기 문구가 남아 있어 알았다).
    그래서 **상영작·날짜·시간은 한 줄도 인용하지 않았다.** 범례만 썼다.
  · 「시네마테크KOFA 온라인 예매 절차 변경 안내」 공지가 존재하는데 **열리지 않았다.**
    영문면은 여전히 5일 전·11시 방식을 적고 있다. 그래서 본문은 **현장 예매**를
    권하는 쪽으로 썼다 — 절차가 바뀌어도 남는 부분이라서.
  · 도보 15분은 **근사값**이다. 오시는 길 페이지가 안 열렸고 색인된 값이다. 그렇게 밝혔다.
  · CGV·롯데·메가박스는 **판정하지 않았다.** 세 곳 다 페이지가 브라우저에서 조립돼
    못 읽었다. 「안 한다」고 쓰지 않고 「우리는 확인하지 못했다」로 남겼다.

■ 사진 — 한 장도 쓰지 않았다
  상암·DMC·한국영상자료원 사진이 재고 242장 중 0장이다(슬러그 전수 확인).
  그리고 이번엔 「아직 못 구했다」가 아니다 — **상영관 안은 촬영 금지이고 그 규칙이
  본문에 인용돼 있다.** 우리가 정당하게 가질 수 있는 사진이 아니다.
  외관 사진은 「그냥 사무용 건물」, 다른 서울 사진은 「그냥 한국」이 된다.
  HERO_EXEMPT 에 사유를 적고 **2×2 도표(ChartSubtitleGrid)로 지탱했다.**
  가을편·병원 간판편과 같은 판단.

■ 도표는 빌드 통과 뒤 실제로 열어 봤고, 두 군데가 깨져 있었다
  ① 아래 행과 맺음 문장이 겹쳤다 (H=330 이 모자랐다) → 386 으로 올렸다
  ② 오른쪽 아래 칸을 fill-opacity 13% 로 깔고 **같은 색 글자**를 얹어서,
     옅은 색을 무시하는 렌더러에서는 글자가 통째로 사라졌다
     → 불투명도 대신 옅은 색을 직접 지정하고 글자를 진하게 바꿨다.
  **빌드는 두 번 다 통과했다.** 눈으로 안 봤으면 그대로 나갔다.

■ 스키마 판단 하나
  sceneKind 를 **culture** 로 두었다. 갈 곳이 있는데도 그렇게 한 이유는,
  location 이 강제하는 claimWording 여섯 값이 전부 「작품이 여기서 찍혔다」 계열이라
  영화관에는 맞는 값이 없고, photoTips(어디 서서·어느 방향으로)도 지하 상영관에는
  뜻이 없기 때문이다. 주소·교통·휴관일은 본문에 그대로 넣었다.

■ 함께 바뀐 파일
  · src/components/ChartSubtitleGrid.astro          (새 도표)
  · src/integrations/content-quality.mjs            (HERO_EXEMPT 에 사유 추가)
  · data/stuck.json                                 (「자막이 하나도 안 나온다」 항목 + 일·중 번역)
  · public/og/scenes__…__en.png                     (복사본에서 만들어 손으로 옮김)
```

---

## 검증 기록

지시문 ⑥-A 대로 **마운트된 폴더가 아니라 `/tmp` 복사본에서만** 돌렸습니다.
실제 폴더에서는 `npm`·`npx` 를 한 번도 실행하지 않았습니다.

```
npx astro build            통과 — 112 pages
  no-shared-images         사진 103장 · 중복 없음
  no-duplicate-files       사진 242장 · 내용 중복 없음
  photo-sanity             사진 장소 40곳 · 이름 검사 통과 · 확인 대장 최신
  content-quality          발행 기준 통과 (대표사진 예외 16건 — 사유 기재됨)
node scripts/check-i18n.mjs        112쪽 · canonical·hreflang·제휴추적·sponsored 전부 통과
node scripts/link-check.mjs        67편 · 주소 157개 · 통과
```

경고 30건은 전부 **기존 기사**의 것입니다(같은 장소 사진 반복, 본문 도표 없음,
2026-08-16 이전 31편 중 28편 절 구성 동일). 이번 기사에서 난 경고는 없습니다.

---

## 다음 실행분에 남기는 메모

- **번역 부채**: 이 기사는 영어 원문만 있습니다. 표준순서(영어→일→중)대로라면
  음악방송 편 다음 차례가 됩니다. 다만 지시문 ⑫ 대로 `LOCALE_CONTENT_PLAN` 을
  먼저 보십시오 — 한자를 읽는 독자에게 「자막이 없다」는 벽의 높이가 다릅니다.
- **사진 요청**: `data/photo-requests.json` 에 **상암 DMC 단지 / 한국영상자료원 외관**을
  올려 두면, 도착했을 때 HERO_EXEMPT 에서 이 줄을 지울 수 있습니다.
  (이번 실행에서는 요청 파일을 건드리지 않았습니다.)
- **90일 재확인**: 오늘은 수요일이라 ⑧·⑨ 대상이 아니었습니다. 다음 월요일 실행분에서
  `node scripts/weekly-plan.mjs` 를 먼저 돌리십시오.
