# 2026-08-24 (월) 실행분 — **발행 0편.** 셸이 죽어서 게이트를 못 돌렸다

```
chore: 월요일 재확인 + 재료 확보 (발행 없음 — 빌드 불가)

축 균형·90일 경과·번역 부채를 파일에서 다시 세었다. 셋 다 이상 없다.
새 기사는 쓰지 않았다. 샌드박스 셸이 죽어 게이트 넷을 돌릴 수 없었고,
⑦-0 은 게이트 통과 전에는 src/content 에 넣지 말라고 한다.

확인한 것
  · 90일 경과 요금·규칙 기사 0편 (최고령 checkedAt 2026-08-13, 11일)
  · 번역본 28편 전부 sourceCheckedAt 이 원문 checkedAt 과 일치. 부채 0건
  · 본문 54편 축 계산 — Hangul·Decode 동률 최미달(각 −4.8편), Now 여전히 과잉(+9.2)

확인하지 못한 것
  · npx astro build 및 게이트 넷 — 셸 없음
  · weekly-plan.mjs — 셸 없음. 축 계산은 Glob 으로 손으로 대신했다
  · kobus.co.kr · humetro.busan.kr — 못 열었다 (아래)

찾은 것 (기사 아님, _검증대기-2026-08-24.md 에)
  · KTO 영문 Transportation Cards 페이지가 자기 안에서 모순된다.
    비교표는 기후동행카드를 「서울 내」로, FAQ Q5 는 「Seoul lines + Gimpo Gold Line」으로 적었다.
    서울시 원문 확인 결과 단기권 이용범위는 서울시다 — 우리 기사가 맞고 KTO 영문이 틀렸다.
    english-pages-korea-out-of-date 편에 날짜 박힌 사례로 넣을 것.

걸러낸 사진: 없음 (사진 작업 자체를 못 했다 — photo-audit.py 가 셸을 쓴다)
```

---

## 무슨 일이 있었나

`mcp__workspace__bash` 가 **다섯 번 연속 같은 오류로 죽었다.**

```
ensure user: useradd failed: exit status 1:
useradd: /etc/passwd.20877: No space left on device
```

샌드박스 리눅스의 디스크가 찼다. 사용자 계정 생성 단계에서 터지므로
**명령을 하나도 실행할 수 없다.** 다섯 번째 시도 뒤 도구가 「재시도해도 소용없다」고 알렸고 멈췄다.

### 그래서 못 한 것

| 지시문 | 무엇 | 왜 못 했나 |
|---|---|---|
| ⑪ | `node scripts/weekly-plan.mjs` — 월요일 첫 단계 | 셸 |
| ⑥-A · ⑥ | `/tmp` 복사본에서 `npx astro build` + 게이트 넷 | 셸 |
| ② | `photo-audit.py`, 대조표 PNG 만들어 Read 로 열기 | 셸 |
| ⑥ | 도표 SVG → PNG 렌더해서 눈으로 확인 | 셸 |

### 그래서 발행하지 않았다

⑦-0 은 이렇게 적혀 있다.

```
게이트 넷 + check-i18n + link-check 통과  →  src/content/<축>/ 에 바로 넣는다
하나라도 실패                              →  고치기 전에는 넣지 않는다
```

**나는 「실패」한 것이 아니라 「돌리지도 못한」 상태다.** 어느 쪽이든 넣으면 안 되는 자리다.
게다가 이 리포는 `auto-push.ps1` 이 10분마다 `src/content/` 를 보고 있어서,
검증 안 된 원고를 거기 두면 **내 판단이 아니라 자동화가 발행해 버린다.**

⑧ 이 정확히 이 경우를 다룬다. **틀린 글 한 편이 안 쓴 열 편보다 나쁘다.**

### `_drafts/` 에도 두지 않았다

⑦-0 은 `_drafts/` 를 **발행 대기실이 아니라 보류함**으로 정의하고, 들어갈 것을 둘로 한정한다 —
주제 판단이 틀려 접은 글, 사실을 못 채워 멈춘 글. **검증을 못 돌린 완성 원고는 그 둘이 아니다.**
초안만 쌓이고 발행이 0이던 8월 20일을 반복하지 않는 편이 낫다고 봤다.

---

## 대신 한 것

셸 없이 할 수 있는 일 중 값이 큰 순서로 했다. Read·Glob·Grep·웹 검색은 살아 있었다.

**① 월요일 재확인을 손으로 대신했다** (⑨·⑪). 결과는 위 커밋 메시지에 있고 상세는 검증대기 파일에 있다.
셋 다 깨끗해서, 셸이 살아 있었더라도 오늘 재확인 작업량은 0이었을 것이다.

**② ①-B 채굴을 돌렸다.** 발행 위험이 없고 다음 실행분이 바로 쓸 재료가 된다.
여기서 나온 것이 KTO 영문 페이지 모순이다. 우리 기사와 KTO 를 나란히 놓고 대조하다 나왔고,
②-C 가 요구하는 「우리만 아는 사실」의 전형이다 — 자료를 정리한 것이 아니라 **자료끼리 어긋나는 곳**이다.

**③ B그룹 후보 둘에 stakes 시험을 붙여 두었다.** 고속버스/시외버스 편(Hangul·최미달 축·stakes 통과)과
부산 도착 후 교통카드 편. 둘 다 **⬜ 이고 1차 출처를 아직 못 열었다.**

---

## 운영자가 해 주셔야 하는 것

**세션을 새로 열어 주십시오.** 샌드박스 디스크가 차서 이 세션에서는 복구할 방법이 없습니다.
새 세션이면 셸이 정상으로 돌아옵니다.

윈도우 쪽은 **아무것도 건드리지 않았습니다.** ⑥-A 사고(node_modules 파손) 같은 일은 없었습니다 —
npm·npx 를 애초에 실행할 수 없었기 때문입니다. `.git` 잠금 파일도 만들지 않았습니다.
리포에 들어간 변경은 **이 파일과 `_검증대기-2026-08-24.md` 두 개뿐**이고 둘 다 `src/` 밖입니다.

## 다음 실행분이 먼저 할 것

1. **기후동행카드 30일권 충전 마감이 8월 31일 — 7일 남았다.** 기사는 이미 맞게 쓰여 있으므로
   고칠 것은 없지만, 8월 31일과 10월 1일에 그 기사의 시제를 손봐야 한다.
2. KTO 영문 페이지를 **다시 열어** 모순이 아직 있는지 보고, 있으면
   `english-pages-korea-out-of-date` 편에 날짜와 함께 넣는다. 고쳐졌으면 고쳐졌다고 쓴다.
3. 새 원문은 **Hangul 축** — 고속버스/시외버스 편. `kobus.co.kr` 이 열리는지가 관문이다.
   안 열리면 그 기사는 접고 Decode 로 간다.
