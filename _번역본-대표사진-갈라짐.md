# 번역본 대표사진이 원문과 갈라진다 (미해결)

2026-08-20 이태원 편에 사진을 넣다가 드러났다. **아직 안 고쳤다.**

## 규칙과 코드가 다르다

`src/integrations/content-quality.mjs` 대표사진 검사의 주석은 이렇게 선언해 놓았다.

> 번역본은 **같은 기사의 다른 언어판**이지 새 기사가 아니다.
> 사진을 frontmatter 에 복사해 두면 원문에서 사진을 바꿀 때
> 번역본만 옛 사진을 가리키게 된다. **사진은 한 곳에만 적는다.**

그런데 `src/pages/[lang]/scenes/[slug].astro` 는 `d.hero` 를 **그 문서에서만** 읽는다.
`translation.of` 를 따라 원문에서 물려받는 코드가 없다. decode·hangul·now·guides 도 같다.

그래서 실제로는 이렇게 된다.

| | 결과 |
| --- | --- |
| 번역본에 hero 를 **안 적으면** | 일본어·중국어 페이지에 대표사진이 안 나온다 |
| **적으면** | 나오지만, 원문에서 바꿀 때 번역본이 옛 사진에 남는다 |

주석이 경고한 사고를 피하려면 사진이 안 나오고, 사진을 내보내려면 그 사고를 떠안는다.

## 실제로 났다

이태원 편 대표사진을 `itaewon-2` → `itaewon-4` 로 바꾸면서 **파일 세 개를 손으로 다 고쳤다.**
하나라도 빠뜨렸으면 일본 독자만 다른 사진을 보게 된다. 그리고 **어떤 게이트도 그걸 안 잡는다** —
`check-i18n.mjs` 는 hero 를 아예 보지 않는다 (2026-08-20 확인).

## 먼저 — 사진을 공유하는 것 자체는 문제가 아니다

2026-08-20 운영자 질문: "세 언어판이 사진을 공유하면 무슨 문제가 있나?"

**없다. 그게 맞다.** 독자는 셋 중 하나만 본다. 일본 독자와 영어 독자가 같은 사진을
두 번 보는 일은 일어나지 않는다. 파일도 하나, 브라우저가 받는 주소도 하나다.

문제는 **같은 사실을 세 군데에 적어 둔 것**이다. 사진을 바꿀 때 세 번 고쳐야 하고,
하나 빠뜨려도 잡아 줄 게이트가 없다.

## 어떻게 고칠까 — 통째로 물려받으면 안 된다

처음엔 "번역본 hero 를 지우고 원문에서 물려받자" 로 적었는데 **그건 틀렸다.**
그러면 alt 까지 물려받아서 일본어 페이지에 영어 alt 가 붙는다.
화면 낭독기에도, 이미지 검색에도 손해다.

갈라야 한다.

| | 어디에 |
| --- | --- |
| 파일 · 라이선스 · sourceUrl · 크레딧 | **원문 한 곳.** 사실이고 언어와 무관하다 |
| alt · 캡션 | **언어마다 따로.** 이건 사실이 아니라 글이다 |

- 각 `[slug].astro` 에서 `d.translation?.of` 가 있으면 원문 항목의 사진 파일·출처를 쓰고,
  alt 는 번역본 것을 쓴다
- 번역본 frontmatter 는 `hero.alt` 만 남긴다 (스키마도 그에 맞게 나눠야 한다)
- 게이트를 하나 더 두는 방법도 있다 — 번역본 hero 파일이 원문과 다르면 빌드를 세운다.
  다만 그건 증상을 막는 것이고, 위가 원인을 없앤다.

**지금은 안 건드린다.** 템플릿 다섯 개를 동시에 손대는 일이고, 이태원 편이 방금 초록불로
나갔다. 다음에 사진 있는 기사를 번역하기 **전에** 하면 된다. 그 전까지는 손으로 맞춘다.
