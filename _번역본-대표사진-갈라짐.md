# 번역본 대표사진이 원문과 갈라진다 — **고쳤다 (2026-08-22)**

2026-08-20 이태원 편에 사진을 넣다가 드러났고, 2026-08-22 에 고쳤다.
이 문서는 무엇이 잘못돼 있었고 무엇으로 바꿨는지의 기록이다.

## 무엇이 잘못돼 있었나

`src/integrations/content-quality.mjs` 대표사진 검사의 주석은 이렇게 선언해 놓았다.

> 번역본은 **같은 기사의 다른 언어판**이지 새 기사가 아니다.
> 사진을 frontmatter 에 복사해 두면 원문에서 사진을 바꿀 때
> 번역본만 옛 사진을 가리키게 된다. **사진은 한 곳에만 적는다.**

그런데 **어떤 코드도 그 규칙을 실행하지 않았다.** 각 `[slug].astro` 는 `d.hero` 를
그 문서에서만 읽었고, `translation.of` 를 따라 원문에서 물려받는 코드가 없었다.

그래서 번역본 18쪽이 두 갈래로 잘못돼 있었다.

| | 쪽수 | 결과 |
| --- | --- | --- |
| 사진을 **안 적은** 번역본 | 9 | 일본어·중국어 페이지에 대표사진이 **아예 없었다** (og:image 도) |
| **적은** 번역본 | 9 | 나오지만 같은 사실이 세 군데 — 원문에서 바꾸면 갈라진다 |

사진이 없던 9쪽: 경주 야경 ja·zh · 인천공항 ja·zh · 식당 간판 ja · 한글 읽기 zh ·
왕궁 무료입장 ja·zh · 북촌 zh.

실제로 사고가 날 뻔했다 — 이태원 편 대표사진을 `itaewon-2` → `itaewon-4` 로 바꾸면서
**파일 세 개를 손으로 다 고쳤다.** 하나라도 빠뜨렸으면 일본 독자만 다른 사진을 봤을 것이고,
`check-i18n.mjs` 는 hero 를 아예 보지 않는다.

## 사진을 공유하는 것 자체는 문제가 아니었다

2026-08-20 운영자 질문: "세 언어판이 사진을 공유하면 무슨 문제가 있나?"

**없다. 그게 맞다.** 독자는 셋 중 하나만 본다. 파일도 하나, 브라우저가 받는 주소도 하나다.
문제는 **같은 사실을 세 군데에 적어 둔 것**이었다.

## 어떻게 고쳤나 — 통째로 물려받지 않고 갈랐다

처음엔 "번역본 hero 를 지우고 원문에서 물려받자" 로 적었는데 **그건 틀렸다.**
그러면 alt 까지 물려받아 일본어 페이지에 영어 alt 가 붙는다.
화면 낭독기에도, 이미지 검색에도 손해다. 그래서 갈랐다.

| | 어디에 |
| --- | --- |
| 파일 · 라이선스 · sourceUrl · 크레딧 | **원문 한 곳.** 사실이고 언어와 무관하다 |
| alt | **언어마다 따로.** 이건 사실이 아니라 글이다 |

### 바꾼 것

- `src/config/hero.mjs` (새 파일) — `makeHeroResolver(entries, section)`.
  원문이면 자기 hero, 번역본이면 원문 hero 에 자기 `heroAlt` 만 얹는다.
- `src/content.config.mjs` — `heroAlt` 필드 추가. `hero` 는 나머지 전부 필수인 채로 둔다.
  **원문 쪽 규율은 느슨하게 하지 않았다.**
- `[lang]/{now,guides,decode,hangul,scenes}/[slug].astro` — `getStaticPaths` 에서 해결해
  props 로 넘긴다. 본문·JSON-LD·og:image 세 곳이 같은 값을 쓴다.
- `[lang]/index.astro` — 홈도 같은 버그가 있었다. 언어로 거르기 **전에** 해결해야 해서
  `getCollection` 의 필터를 옮겼다.
- 번역본 18개 파일 — 9개는 `hero:` 블록을 지우고 `heroAlt:` 로, 9개는 `heroAlt:` 를 새로 썼다.

### 새 게이트 (content-quality 1-b)

- 번역본에 `hero:` 가 있으면 **빌드를 세운다.**
- 원문에 사진이 있는데 번역본에 `heroAlt:` 가 없으면 **빌드를 세운다.**
  영어 alt 로 조용히 떨어뜨리지 않는다 — `ui.mjs` 의 `t()` 가
  「영어로 대체하지 않습니다」라고 던지는 것과 같은 이유다.

## 남은 것

- 새로 쓴 `heroAlt` 9건은 **영어 alt 를 옮긴 것**이다. 그 영어 alt 는 사진을 열어 확인한
  것이지만(`data/photo-verified.json`), 번역문 자체를 사진과 대조하지는 않았다.
  일본어·중국어 검수자가 생기면 한 번 훑을 것.
