# 두 세션이 같은 저장소를 만질 때의 규칙

2026-08-17 작성. 이날 실제로 두 번 부딪혔고, 둘 다 막을 수 있는 종류였다.

이 저장소에는 지금 **두 작업 흐름**이 있다.

- **자동화 세션** — 수집기, 링크 점검, GitHub Actions
- **집필 세션** — 기사, 컴포넌트, 다국어, 화면

한쪽만 일하게 만들 필요는 없다. 오늘 부딪힌 두 건은 성격이 달랐고, 규칙 세 줄이면 둘 다 안 생긴다.

---

## 오늘 실제로 일어난 일

### ① 파일 충돌 — 같은 사진을 두 이름으로

관광공사 수집기가 **이미 저장소에 있던 사진 13장을 다시 받아**
콘텐츠 ID 이름(`131271-*.jpg`)으로 저장했다.
기존 이름은 `jumunjin-breakwater-*.jpg` 였고, 기사가 쓰는 건 그쪽이었다.

내용이 같은 파일 13쌍 = 4.3MB.
`no-duplicate-files` 게이트가 잡아서 **빌드가 멈췄다.**
멈췄다는 건 그 시점부터 **양쪽 다 배포가 안 됐다**는 뜻이다.

> 게이트가 일한 것이다. 없었으면 4.3MB 를 계속 끌고 다녔을 것이다.

### ② 타이밍 충돌 — `.git/index.lock`

두 프로세스가 동시에 git 을 만졌다.
GitHub Desktop 에 `A lock file already exists in the repository` 가 떴고,
커밋이 통째로 실패했다.

---

## 규칙 셋

### 1. 커밋·푸시는 한 번에 한쪽만

`index.lock` 오류의 원인은 이것뿐이다.
한쪽이 커밋하는 동안 다른 쪽은 **파일만 고치고 기다린다.**

락이 남아서 안 풀리면:

```
cd C:\Users\user\hankukin
del .git\index.lock
```

### 2. 푸시 전에 반드시 Pull

오늘 병합이 꼬인 것도 이것 때문이다.
Pull → (충돌 있으면 해결) → Push. 순서를 건너뛰지 않는다.

### 3. 영역을 나눈다

| | 담당 |
| --- | --- |
| `scripts/` · `.github/workflows/` · `data/` 의 **수집 결과물** | 자동화 세션 |
| `src/content/` · `src/components/` · `src/config/` · `src/pages/` | 집필 세션 |
| `data/stuck.json` · `data/ledger.json` · `data/comment-tally.json` | 집필 세션 (사람이 쓰는 데이터) |
| `src/assets/images/` | **양쪽** — 그래서 여기가 제일 위험하다 |

`src/assets/images/` 에 새 파일을 넣는 쪽은,
**이미 같은 사진이 다른 이름으로 있는지 먼저 확인한다.**
수집 스크립트라면 내용 해시로 비교해서 이미 있으면 받지 않는다.

---

## 그물은 CI 다

`.github/workflows/verify.yml` 이 푸시마다 `astro build` 를 돌린다.
게이트 열세 개가 전부 거기서 다시 돈다.

오늘 중복 사진을 잡은 것도, 메일을 보낸 것도 이 워크플로다.
**두 세션을 병행하는 한, 이게 제일 중요한 안전장치다.**

메일로 `verify: All jobs have failed` 가 오면:

1. 그 시점부터 **배포가 멈춰 있다**고 생각한다
2. 워크플로 로그를 열어 어느 게이트인지 본다
3. 고치기 전까지는 양쪽 다 새 푸시를 얹지 않는다

---

## 게이트를 우회하지 않는다

2026-08-16 에 로컬 스크립트가 npm 을 못 찾자 빌드를 건너뛰고
커밋 메시지에 `UNVERIFIED` 라고 **정직하게 적은 뒤 그대로 푸시했다.**
게이트 여섯 개가 우회됐고 아무 소리도 나지 않았다.

정직하게 적는 것으로는 부족하다. 그래서 검증을 로컬이 아니라 저장소 쪽에 뒀다.
**빌드가 안 되면 푸시하지 않는다** — 어느 세션이든.
