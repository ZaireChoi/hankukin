# 커밋 메시지 — 2026-09-07 (월) 오후 실행분

```
멈춰 있던 자동 푸시를 되살리고, Scenes 최미달 축에 성산일출봉 편을 넣는다

VERIFIED - astro build 통과 (147 pages) · check-i18n 146쪽 통과 · link-check 통과
           게이트 넷 전부 통과 (경고 39건, 전부 기존 부채)

■ 가장 급한 것부터 — 사이트가 12:50부터 발행을 멈추고 있었다 ★
  .auto-push.log 가 12:50 이후 열 번 연속 「중단: 빌드 실패」다.
  아침 실행분의 결과물이 커밋만 되고 **한 번도 올라가지 못한 상태**였다.

  원인은 아침에 추가한 블록 주석이다. MDX 에서 이렇게 쓰면 안 된다:

      import ddp from '…/ddp-1.jpg';
      ⟵ 빈 줄
      /* … 주석 … */
      export const KTO = {

  빈 줄이 주석을 **ESM 블록에서 떼어 낸다.** 그러면 주석은 그냥 문단이 되고,
  바로 다음 줄의 `export const KTO = {` 도 그 문단의 이어짐이 되어
  `{` 가 **MDX 표현식으로** 파싱된다. acorn 이 `credit:` 의 콜론에서 죽는다
  (`50:9  Unexpected content after expression`).

  세 파일이 같은 모양이었다. 빌드는 첫 파일에서 서므로 하나만 보였을 뿐이다.
    now/seoul-transport-fares-subway-bus-taxi.mdx
    now/ja/…  ·  now/zh-hans/…          ← 번역본 둘도 같은 사고

  고친 방식: **`/*` 앞의 빈 줄을 없앤다.** 그러면 주석이 import 들과 같은 ESM
  블록 안에 들어가고 `*/` 다음의 export 도 같은 블록으로 이어져 정상 JS 가 된다.
  거제 편(scenes/geoje-island-what-to-see.mdx)이 이미 그 모양이라 멀쩡했다 —
  **같은 날 같은 손이 쓴 주석인데 한쪽만 터진 이유가 빈 줄 하나였다.**

  ▸ 반대로 「`*/` 다음에 빈 줄을 넣는」 방식은 쓰면 안 된다. 그러면 빌드는 통과하지만
    한국어 주석이 **화면에 문단으로 그대로 나간다.** dist 를 grep 해서 확인했다(0건).

  CLAUDE.md 에 「블록 주석 안에서 별표 뒤에 슬래시를 붙이지 않는다」가 있는데,
  이건 그 항목의 이웃이다. 주석 자체가 아니라 **주석의 앞뒤 빈 줄**이 문제다.

■ 그리고 빌드를 되살렸다 — 아침의 「디스크가 꽉 찼다」는 절반만 맞았다 ★
  `$HOME`(/sessions)은 정말 못 쓴다. 지난 세션 폴더 **464개**가 nobody 소유로
  9.0G/9.8G 를 먹고 있고 rm 이 거부한다(직접 시도해서 확인). 이건 운영자 몫이다.

  그런데 `/`(/dev/sda1)에는 500MB 가 있었고 **거기서는 된다.**
  tar 가 죽은 진짜 이유는 옮기던 것 안에 이게 있었기 때문이다:

      _to_delete   200MB   ← 복사본 223MB 중 200MB. 이름 그대로 지울 것들이다
      src/assets    82MB   ← 심볼릭 링크로 대신할 수 있다

  둘을 빼니 복사본이 **23MB** 가 되고 npm ci(190MB)와 dist 가 넉넉히 들어갔다.
  사진은 `ln -s` 로 마운트를 가리키게 했다 — 게이트도 <Image> 도 원본을 그대로 읽고,
  빌드는 원본을 안 건드리므로 마운트가 위험해지지 않는다.
  **정확한 명령을 `_자동화-지시문.md` ⑥-A 에 적어 두었다.** 다음 실행분이 다시 헤매지 않도록.
  끝나고 `/tmp/hk` 는 지웠다 — 우리 uid 소유라 지울 수 있다. 이걸 안 지운 것이
  `/var/tmp` 열 벌이 쌓인 경위다.

■ 월요일 순서 (⑪) — ①번역 부채 없음 ②90일 경과 0건
  최고령 checkedAt 이 2026-08-13(25일). 아침 실행분의 판정과 같다.
  ④ 최미달 축 Scenes 17/20.7(−3.7) → 여기를 채웠다.

■ 새 글 — scenes/seongsan-ilchulbong-hours-fee-sunrise.mdx (1,292단어)
  아침 실행분이 `_초안/성산일출봉-자료-2026-09-07.md` 에 남긴 자료로 썼다.
  다만 **숫자는 전부 내가 다시 열어서 확인했다.** 그 결과 자료 한 줄이 틀렸다(아래).

  stakes: 새벽 4시에 일어나 섬을 가로질러 갔는데 문이 닫혀 있다 — 시간·돈·한 번뿐인 일출.

  확인한 것 (제주특별자치도 세계유산본부, Chrome + JS. web_fetch 로는 탭이 안 열린다):
   · 휴관일 **매달 첫 번째 월요일** ← 영어권 페이지는 대개 연중무휴로 적는다.
     공교롭게 발행일 2026-09-07 이 9월의 첫 월요일이다. 그 사실로 글을 연다
   · 관람시간 3구간 + **매표마감이 폐장 한 시간 전**
   · `*필요시에는 관람시간을 따로 정할 수 있음` — 표는 약속이 아니라 계획이다
   · 요금표: **어른이 25세부터 64세.** 24세는 청소년 요금이다
   · 표에 **6세 이하·65세 이상 행이 아예 없다** → 무료일 가능성이 높지만 안 적는다
   · 대중교통 소요시간 70/68/57분, 1132 지방도, 성산 하차
   · 일출시각은 **성산일출봉에서 잰 값**(`※ 성산일출봉 기준`)

  ★ 아침 자료의 오류를 잡았다 — 「2026년 4~9월치가 있다」가 아니다.
    2026년 열두 달을 하나씩 열어 봤다:

        1~3월 없음 · **4~7월 있음** · **8월 없음** · 9월 있음 · 10~12월 없음
        2025년 10월도 없음 (뒤에서 채워지는 표가 아니다)

    **8월만 한가운데가 비어 있다.** 한국에서 가장 붐비는 달이다.
    이유는 모르고 지어내지 않았다. 「비어 있다」고만 적었다.
    아침 자료를 그대로 옮겼으면 8월 값이 있는 것처럼 쓸 뻔했다.

  ★ 우리만 있는 것 — 두 페이지를 겹쳐서 표를 만들었다
    같은 기관의 관람시간표와 일출표를 나란히 놓으면 「문 열고부터 해 뜰 때까지」가 나온다.
    **4월 말이 46분으로 발표된 한 해 중 가장 빡빡하다** (5월 54 · 6월 52 · 7월 56 · 9월 66~86).
    영어권에 이 계산이 없다. 두 페이지 다 한국어뿐이기 때문이다.

  ❌ 쓰지 않은 것과 그 이유
   · **정상까지 걸리는 시간.** 아침 자료의 근거였던 VisitJeju 가 오늘 DNS 자체가 죽어
     (`DNS_PROBE_FINISHED_NXDOMAIN`, www 있이·없이 둘 다) **내 눈으로 못 열었다.**
     검색해 보니 15분·30분·40분·왕복 50분·1시간반이 다 나온다 — 즉 정설이 없다.
     그래서 숫자를 안 쓰고 「기관이 안 적는다, 그러니 창 전체를 예산으로 잡아라」로 썼다
   · **첫차 시각과 버스 요금.** 제주버스정보시스템이 정류소·노선은 주는데 첫차를 안 준다.
     결론(「성산에서 자라」)은 이 숫자 없이도 선다 — 70분 소요면 9월엔 03:45에 떠나야 한다
   · 「무료 구간이 따로 있다」도 뺐다. 근거가 VisitJeju 였고 오늘 못 열었다.
     대신 **사진 넉 장을 근거로** 썼다 — 넷 중 셋이 봉우리 «밖»에서 찍은 것이고,
     엽서 사진은 봉우리 위에서는 찍을 수 없다. 이건 사진이 스스로 보여주는 사실이다

■ 사진 — 대조표를 다시 만들어 네 장을 눈으로 열었다 (오매칭 0)
  hero  -4 항공. 초록 분화구 사발 + 육계사주 + 뒤쪽 마을·항구. **5,000원이 사는 것이 이것**
  본문  -2 자전거 차선 그어진 굽은 해안도로에서 만 건너 봉우리 (접근 절)
  본문  -1 콘크리트 사각 개구부가 봉우리를 액자처럼 잡은 전망 구조물
           → 8월에는 「구조물이 화면 절반」이라 안 썼는데, 이번 기사에서는
             **그 구조물이 곧 요점**이라 실었다. 어느 건물인지는 모르므로 이름을 안 붙였다
  제외  -3 일출 사진 — 제주 편 대표사진으로 이미 쓰는 중 (no-shared-images)

  **넷 다 kogl-3(변경금지)** 로 선언했다. 아침에 고친 그 사고를 반복하지 않으려고
  mdx 상단 주석에도 이유를 적어 두었다. photo-verified 대장도 오늘 날짜로 갱신했다.

■ 게이트가 한 번 울었고, 그게 맞았다
  첫 빌드에서 content-quality 가 세웠다:
  「연속 4편이 똑같은 절로 끝납니다 — what this article does not claim」

  **우회하지 않았다.** 제목만 바꾸면 통과하지만 그건 게이트를 끄는 것과 같다.
  고지 절을 읽어 보니 본문에 이미 있는 말의 요약이었다 — 그래서 **통째로 뺐다.**
  대신 고지가 필요한 자리에 흩어 넣었다:
    · 요금표 바로 밑에 「이 날짜의 요금이다」
    · 관람시간표 바로 밑에 「게이트의 안내판이 이 표보다 위다」
    · 못 구한 것들은 각자 그 절 안에서
  글은 이제 「제주시에서는 못 한다」로 끝난다. 실행 가능한 문장으로 끝나는 편이 낫다.

■ 같이 넣은 것
  data/stuck.json  「You are about to do something that has a rule」 그룹에 한 줄
  data/photo-verified.json  jeju-seongsan-ilchulbong 갱신 (2026-08-20 → 09-07)
  공유 카드는 만들 것이 없다 — 대표사진이 있으면 그 사진이 카드가 된다 (make-og 확인)

■ 확인하지 못한 것 — 숨기지 않는다
  · **브라우저로 눈으로 못 봤다.** dist 가 샌드박스 안에 있어 Chrome 이 못 닿는다.
    대신 dist HTML 을 직접 뒤졌다 — 표 4개 정상, `<strong>` 정상, 노출된 `**` 0건,
    한국어 주석 누출 0건, 이미지 셋 다 원본 그대로(리사이즈 없음). **CSS 는 못 봤다**
  · 정상 소요시간·첫차·버스요금·65세 이상 무료 여부 (위)

■ 오늘 새로 배운 것 하나 더 — `git status` 도 락을 남긴다
  ⑥-A 표가 `git status` 를 오래 「해도 되는 것」에 넣어 두고 있었는데 **틀렸다.**
  인덱스를 갱신하느라 `.git/index.lock` 을 만들고, 마운트에서는 그걸 못 지운다
  (`Operation not permitted`). 그대로 두면 다음 자동 푸시의 `git add` 가 선다.

  **`unlink` 는 막혔지만 `rename` 은 된다.** 오늘 것은
  `.git/index.lock.stale-20260907-1433` 으로 이름을 바꿔 풀었고, git 은 정상 동작을
  확인했다. `_자동화-지시문.md` ⑥-A 에 방법과 함께 적어 두었다.
  `.git/` 에 같은 흔적이 아홉 개 더 있다 — 전부 지난 세션들이 남긴 것이다.

■ 자동 푸시는 이미 되살아났다
  14:20 「완료: auto(content): 4 files - verified」 ← MDX 수정분
  14:30 「완료: auto(content): 3 files - verified」 ← 성산일출봉 편·stuck·photo-verified
  12:50~14:10 의 열 번 연속 실패가 여기서 끊겼다.

■ 운영자께
  1. `npm.cmd run og && npm.cmd run verify` 를 한 번 돌려 주십시오.
     (자동 푸시는 이미 정상으로 돌아왔습니다 — 위 참조.)
  2. **`_to_delete` 200MB 를 지워 주십시오.** 샌드박스 복사본 문제의 절반이 이겁니다.
  3. `/sessions` 의 지난 세션 폴더 464개는 여전히 nobody 소유라 이쪽에서 못 지웁니다.
     오늘은 `/tmp` 로 우회했지만 근본 해결은 새 샌드박스입니다.
  4. 성산일출봉 편을 브라우저로 한 번 열어 봐 주십시오 — 표가 넷입니다.
```
