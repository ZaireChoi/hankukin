# 커밋 메시지 — 2026-08-21

**발행 0편. 원고 1편을 `_drafts/` 에 두고 멈췄습니다.**

멈춘 이유는 주제도 사실도 아닙니다. **리눅스 샌드박스가 죽었습니다.**

```
bash failed on resume, create, and re-resume.
useradd: /etc/passwd.57548: No space left on device
```

다섯 번 연속 같은 오류였고 다섯 번째에 도구가 「더 시도하지 말라」고 회신했습니다.
그래서 `npx astro build` 를 **한 번도 돌리지 못했습니다.** 게이트 넷도, `check-i18n` 도,
`link-check` 도, `make-og.mjs` 도, 도표 PNG 렌더 확인도 전부 못 했습니다.

⑤·⑥·⑦-0 은 「게이트를 통과하면 바로 올린다 / 하나라도 실패하면 넣지 않는다」입니다.
**통과도 실패도 아닌 「돌리지 못함」은 통과가 아닙니다.** 그래서 넣지 않았습니다.
⑧ 대로 건너뛰고 이유를 여기 남깁니다.

⑥-A 를 지켰습니다 — 실제 폴더에 `npm`·`npx` 를 **어떤 형태로도 돌리지 않았습니다.**
샌드박스가 없어서 못 돌린 것이기도 하지만, 있었더라도 `/tmp` 복사본에서만 돌렸을 것입니다.
샌드박스에서 `git commit`·`git add` 도 하지 않았습니다 (⑦).

`_drafts/` 는 보류함입니다. 이 원고는 **주제 판단이 틀려서 내린 것도, 사실을 못 채워서
멈춘 것도 아닙니다.** 세 번째 종류입니다 — 검증 환경을 못 써서 멈춘 글.
샌드박스가 살아나면 아래 §5 순서대로 옮기면 바로 발행됩니다.

---

## 1. 무엇을 썼나

```
_drafts/korean-address-road-name-building-number.mdx
_drafts/ChartAddressDistance.astro
```

**Hangul / How to Read a Korean Address: Every Number Is a Distance in Metres**
seriesPart 6 · 약 1,800단어 · 고지 약 150단어(8%) · 내부 링크 4개 · 원문 병기 3개 · 도표 1개

한국 건물번호는 순번이 아니라 **도로 시작지점으로부터의 거리**라는 것,
홀수는 왼쪽·짝수는 오른쪽이라는 것, `3길` 과 `3번길` 이 완전히 다른 체계라는 것,
도로명판 다섯 종류가 각각 무엇을 답하는지, 그리고 도로가 없는 곳을 덮는
국가지점번호 10m 격자.

## 2. 축 — Hangul 을 고른 근거

발행분을 세었습니다 (번역본 제외, 2026-08-21 기준 50편).

| 축 | 현재 | 목표 | 차이 |
|---|---:|---:|---:|
| Scenes | 12 (24%) | 30% | −3편 |
| **Hangul** | **5 (10%)** | **20%** | **−5편** |
| Decode | 5 (10%) | 20% | −5편 |
| Now | 18 (36%) | 20% | +8편 |
| Guides | 10 (20%) | 10% | +5편 |

Hangul 과 Decode 가 −5편으로 동률입니다. 동률 순서가 Scenes → Hangul → Decode 이므로
**Hangul** 입니다. Now 는 과잉이고 오늘 바뀐 요금·제도가 없으므로 쓰지 않았습니다.

## 3. stakes 시험 — 축보다 먼저 봤습니다

```
누가 막히나            주소를 손에 들고 택시에 타거나, 숙소를 걸어서 찾거나,
                       예약·통관 서식에 주소를 적는 사람
어디서 막히나          지도 앱이 도로는 맞게 찍어 주고 **어느 끝인지는 안 알려주는** 그 자리
안 읽으면 무엇을 잃나  시간과 돈 — 6.99km 짜리 도로를 반대 방향으로 걷고,
                       택시 미터가 도는 동안 번지를 찾고, 영문 서식의 순서를 뒤집어 적어
                       배송·예약이 도착하지 않는다. 그리고 도로가 없는 곳에서는 안전
```

소비기한 편이 떨어진 자리가 세 번째 줄이었습니다. 이 주제는 세 줄 다 답이 나옵니다.

## 4. 확인한 것과 확인하지 못한 것

**확인한 것 — 1차 출처 원문을 Chrome 으로 직접 열었습니다.**
법제처는 `document.getElementById('lawService').contentDocument` 로 본문이 그대로 읽힙니다
(계획표 §3 에 적힌 방법 그대로, 오늘도 됩니다).

| 사실 | 출처 | 시행 버전 |
|---|---|---|
| 왼쪽 홀수·오른쪽 짝수, 시작→끝 방향 | 시행령 제7조제4항 | 2026-01-02 (대통령령 35948호) |
| 기초간격 20미터 (길 10m·고속도로 2km 예외) | 시행규칙 제3조 | 2025-12-26 (행안부령 592호) |
| 서→동, 남→북으로 시작지점 설정 | 시행령 제7조제2항제7호다목 | 2026-01-02 |
| 대로 40m·8차로 / 로 12~40m·2~7차로 / 길 그 외 | 시행령 제3조제1항제1호나목 | 2026-01-02 |
| 대로↔로, 로↔길 교체 허용 | 시행령 제8조제2항제1호 단서 | 2026-01-02 |
| 건물번호는 주된 출입구의 기초번호 기준 | 시행령 제23조제1항 | 2026-01-02 |
| 도로명주소 = 도로명 + 건물번호 + 상세주소 | 법 제2조제7호 | 2025-02-21 |
| 국가지점번호 10m 격자 · 문자2 + 4자리 2개 | 시행령 제37조 | 2026-01-02 |
| 도로명 없는 도로에서 100m 이상 · 지상 50cm 이상 구조물 | 시행령 제38조 | 2026-01-02 |
| 일련번호식(3길) vs 기초번호식(3번길) · 가·나·다 분기 | 주소정보누리집 「도로명주소 소개」 | 2026-08-21 열람 |
| 영문 표기 역순 · -daero/-ro/-gil/-beon-gil · -si 생략 | 같은 페이지 | 2026-08-21 열람 |
| 도로명판 다섯 종류의 뜻 (1→359 · 92/96 · 92/250 · 200m) | 같은 페이지 도해 | 2026-08-21 열람 |
| 지하상가는 건물번호 앞에 「지하」 | 같은 페이지 | 2026-08-21 열람 |

**이 글에만 있는 것 — 자료끼리 어긋나는 자리 (②-C 마지막 줄)**

「번호 × 10m」 라는 상수는 **법에도 시행령에도 시행규칙에도 없습니다.**
시행령 제7조제4항(홀짝 배분)과 시행규칙 제3조(20미터)를 **곱해야만** 나옵니다.
그런데 행정안전부는 자기 도해에서는 그 곱셈을 이미 해 놓았습니다 —
「강남대로는 6.99km (699 × 10m)」, 「남은 거리는 1.5km ((250-92) × 10m)」.
**조문에 없는 상수를 안내 도해가 쓰고 있는 것**이고, 영어권 자료에는 이 곱셈이 없습니다.
원문 병기 두 블록을 나란히 놓은 이유가 그것입니다. 대조하면 보입니다.

**하나 더 — 영문 페이지가 죽어 있습니다 (2026-08-21 확인).**
검색엔진이 아직 돌려주는 영문 안내서 주소와, **주소정보누리집 자신의 「EN」 버튼**
(title="영문사이트보기") 이 가는 `/openEngPage.do` 가 둘 다 NotFound 페이지로 떨어집니다.
「영어 페이지는 한국어 페이지의 번역이 아니다」 편에 그대로 이어집니다 — 링크했습니다.

**확인하지 못한 것 — 본문에 적었습니다.**

- 홀수가 북쪽·서쪽이라는 것은 **우리 기하**입니다. 서→동·남→북 규정에서 유도했고,
  같은 조문에 행안부령으로 달리 정할 수 있다는 단서가 있습니다. 예외 빈도는 조사 안 했습니다.
- 도로·주소·표지판을 **현장에서 한 건도 확인하지 않았습니다.**
- 119 가 국가지점번호를 받으면 어떻게 처리하는지 **시험하지 않았습니다.** 그래서 쓰지 않았습니다.
- 국가지점번호판의 **색·모양**은 시행규칙 별표(첨부)라 열지 못했습니다.
  초고에 「yellow plate」라고 썼다가 **지웠습니다** — 못 연 자료를 본 것처럼 쓸 뻔했습니다.
- 시행규칙 별표의 도로명판 규격도 같은 이유로 못 열었습니다. 뜻은 주소정보누리집 도해로 썼습니다.

**걸러낸 사진 — 없습니다. 애초에 쓰지 않았습니다.**
도로명판·건물번호판 사진이 재고 242장에 없습니다 (로마자편이 이미 같은 재고를
대조표로 열어 확인한 기록이 있습니다). 그리고 판 하나를 크게 찍어도 이 기사가 가르치려는
**번호와 거리의 관계**는 안 보입니다. 눈금자 도해가 사진보다 정확합니다 —
로마자편·방(房)편·병원 간판편과 같은 판단입니다.

## 5. 살릴 때 함께 해야 하는 것 — 이걸 안 하면 빌드가 섭니다

샌드박스가 살아난 뒤 **한 번에** 하십시오. 순서대로입니다.

```bash
# ① 원고와 도표를 제자리로
_drafts/korean-address-road-name-building-number.mdx
  → src/content/hangul/korean-address-road-name-building-number.mdx
_drafts/ChartAddressDistance.astro
  → src/components/ChartAddressDistance.astro
```

② `src/integrations/content-quality.mjs` 의 `HERO_EXEMPT` 에 추가 (대표사진이 없습니다):

```js
  'korean-address-road-name-building-number':
    '도로명판·건물번호판 사진이 재고 242장에 없다. 로마자편에서 후보 다섯 묶음을 ' +
    '대조표로 이미 열어 봤고 (부산역→하운드호텔, 이태원→시장 옷가게, 명동·청계광장→거리 풍경), ' +
    '주소 표지판이 크게 찍힌 것은 한 장도 없었다. ' +
    '그리고 이 기사가 가르치는 것은 **번호와 거리의 관계**라서 판 하나를 크게 찍어도 안 보인다 — ' +
    '보이려면 눈금자가 있어야 한다. 로마자편·방(房)편·병원 간판편과 같은 판단. ' +
    '도로명판이 판독 가능하게 찍힌 무인물 거리 사진이 생기면 그때 검토한다.',
```

③ `data/stuck.json` 의 **"You cannot read what is in front of you"** 묶음에 추가
(없으면 빌드가 섭니다). `when` 은 독자의 말로, `answer` 는 클릭 안 해도 도움이 되게:

```json
{
  "when": "The map dropped me on the right road but I cannot find the building number",
  "answer": "The number is a distance — multiply it by ten for metres from the start of the road, and odd numbers are on your left, even on your right.",
  "href": "hangul/korean-address-road-name-building-number"
}
```

※ `stuck.json` 은 지금 손대지 않았습니다. **아직 없는 기사를 가리키는 링크**가 되어
`link-check` 가 잡기 때문입니다. 원고를 옮기는 그 순간에 함께 넣으십시오.

```bash
# ④ 공유 카드 (없으면 열다섯 번째 게이트가 세웁니다)
node scripts/make-og.mjs
#   → public/og/hangul__korean-address-road-name-building-number__en.png
#   복사본에서 그렸으면 리포로 **손으로 옮깁니다** (dist 는 옮기지 않습니다)

# ⑤ 검증 — 반드시 /tmp 복사본에서 (⑥-A)
rm -rf /tmp/hk && mkdir -p /tmp/hk
cd C:/Users/user/hankukin && tar -cf - --exclude=node_modules --exclude=.git \
  --exclude=dist --exclude=.astro . | (cd /tmp/hk && tar -xf -)
cd /tmp/hk && npm ci --no-audit --no-fund
npm run verify

# ⑥ 도표를 눈으로 확인 — 빌드가 통과해도 라벨이 겹칠 수 있습니다
#   dist 에서 ChartAddressDistance 의 SVG 를 꺼내 PNG 로 렌더 → Read 로 엽니다
#   특히 볼 곳: 거리 눈금(0/20/40/60/80 m)이 홀짝 숫자와 겹치지 않는지,
#              계산 상자 두 줄이 상자 밖으로 나가지 않는지 (x=RX+168 에서 시작합니다)
```

## 6. 게이트를 미리 맞춰 둔 것 (돌려 보지는 못했습니다)

| 게이트 | 상태 |
|---|---|
| 대표사진 | ✗ 없음 → §5 ② HERO_EXEMPT 필요 |
| 캡션 없는 `<Figure>` | 해당 없음 (Figure 0개) |
| 본문 시각 요소 | 도표 1개 ✓ |
| seoTitle 길이 | 51자 ✓ (상한 60) |
| 내부 링크 | 4개 ✓ (알파벳·로마자·분실물·영어페이지) |
| 마지막 절 연속 4편 동일 | 직전 3편이 각각 「what we could not check…」·「related」·「what this article does not claim」 → 연속 4편 동일 아님 ✓ |
| 「무엇이 막혔나요」 색인 | ✗ → §5 ③ 필요 |
| 공유 카드 | ✗ → §5 ④ 필요 |
| `<Original>` 120자 상한 | 100 / 53 / 86자 ✓ (세어 봤습니다. 컴포넌트가 던지는 값은 못 봤습니다) |
| 상업 링크 | 넣지 않았습니다. 한글 읽는 법에 예약 버튼을 붙이면 자동 생산물처럼 읽힙니다 (②-D) |

## 7. 오늘 잡은 자기모순 셋 (⑤)

무언가를 고칠 때마다 그 옆을 다시 읽었고, 세 군데가 어긋나 있었습니다.

1. 도입부가 **「40번지에 서서 152번지를 찾는다」**고 썼는데, 뒤에서 152가 짝수라 오른쪽이라고
   적었습니다. 40도 짝수입니다 — **같은 쪽인데 「길 건너」라고 쓴 것**입니다. 39로 고쳤습니다.
2. 도표 `aria-label` 이 초고의 152 예시를 그대로 읽고 있었는데, 도표 본문은 강남대로 699로
   바뀐 뒤였습니다. 화면을 못 보는 독자에게만 옛 판이 남아 있었습니다.
3. 「강남대로 699 = 6.99km」가 도입부·도표·1절에 **세 번** 나왔습니다. 1절에서 뺐습니다.
   같은 이유로 대정로23번길이 도표와 표에 겹쳐 있어 표에서 뺐습니다.

그리고 「도로명판은 노란 판」이라고 쓴 문장을 지웠습니다 — §4 에 적은 대로,
색은 시행규칙 별표에 있고 **저는 그 별표를 열지 못했습니다.**

## 8. 운영자에게 — 부탁드릴 것

**샌드박스 디스크가 찼습니다.** 오늘 어떤 코드도 돌리지 못했습니다.
세션을 새로 여시면 대개 풀립니다. 그때 §5 를 순서대로 돌려 주시면 이 글은 바로 나갑니다.

`.git` 잠금 파일은 오늘 만들지 않았습니다 — 샌드박스에서 git 을 아예 쓰지 않았습니다.

```
VERIFIED - 없음. astro build 를 돌리지 못했습니다 (샌드박스 디스크 부족).
           1차 출처는 전부 원문을 열어 확인했습니다 (§4).
           게이트는 소스 기준으로 맞춰 두었으나 **실행으로 확인하지 않았습니다** (§6).
```

**틀린 글 한 편이 안 쓴 열 편보다 나쁩니다.** 검증을 못 한 글을 「통과했다」고 적는 것은
그보다 더 나쁩니다. 그래서 오늘은 0편입니다.
