# HANKUKIN — 세션마다 먼저 읽는 것

이 파일은 **매 세션 자동으로 읽힌다.** 그래서 여기에는 «항상 참인 것»만 적는다.
그날그날의 진행은 `_작업일지-*.md` 에 적고 여기 적지 않는다.

## 이 사이트가 파는 것

영어권 독자에게 **한국의 실무**를 알려준다. 요금·시간·규정·절차.
경쟁 상대는 다른 여행 블로그가 아니라 **틀린 영어 안내 페이지**다.

그래서 지켜지는 규율이 하나 있고, 그것이 전부다.

> **숫자와 사실은 1차 출처를 내 눈으로 열어 본 것만 쓴다.
> 못 구하면 안 쓴다. 「없다」고 적는다.**

지어낸 한 줄이 사이트 전체의 값어치를 없앤다. 빈칸은 그렇지 않다.

## 자료가 필요할 때 — 지어내기 전에 여기부터

| 주제 | 어디에 |
| --- | --- |
| **글 하나 올리는 법** — 최소 frontmatter, 게이트가 우는 자리 | `_글쓰는법.md` |
| **아직 기사가 아닌 자료** — 사진·영수증·증언 | `_초안/` |
| **서울 지하철** — 역·거리·소요시간·환승·좌표 | `_서울지하철-자료.md` |
| **방언 전반** — 한국민족문화대백과사전. **Chrome 으로만 열린다** (web_fetch 는 provenance 제약). 검색은 `encykorea.aks.ac.kr/Article/Search?query=낱말`, 본문은 `/Article/E00xxxxx`. 4~5초 기다린 뒤 `document.body.innerText`. 확인한 표제어: 동남방언(경상) `E0016323` · **서남방언(전라) `E0027670`** · 제주방언 `E0051419` · 성조 `E0029547` · 영남지방 `E0037274`. ⚠ 동남방언은 본문의 음성기호 일부가 ○(U+25CB)로 깨져 있고, **서남방언은 청자경어법 표가 통째로 빠져 있다** — 본문이 「표로 나타내 보이면 다음과 같다」로 끝나고 빈 `<p></p>` 만 있다. 없는 표를 기억으로 채우지 말 것 | `src/content/decode/busan-gyeongsang-dialect-endings-no-na.mdx` sources |
| **국립국어원 — 말의 판정** (2026-09-10 확보) | 셋 다 **web_fetch 로 그대로 열린다.** ① 온라인가나다 답변 `korean.go.kr/front/onlineQna/onlineQnaView.do?mn_id=216&qna_seq=<번호>` — 규범이 아닌 실제 판정이 여기 있다 (부정 의문문 대답 = 308639·311826). ② 우리말샘 항목 `opendict.korean.go.kr/dictionary/view?sense_no=<번호>` ③ 새국어소식 `korean.go.kr/nkview/nknews/<연월>/<번호>.html` — 옛 해설이지만 전문이 통째로 나온다. ⚠ **표준국어대사전(stdict)만 Chrome 이 필요하다** (web_fetch 는 provenance 제약). ⚠ 우리말샘도 **항목마다 다르다** — 176588 은 열렸고 **325646 은 빈 응답**이었다. 빈 응답을 「없다」로 읽지 말 것. ⚠ 게시판 **질문 글에는 실명이 붙어 있다** — 인용하지 말고 국어원 **답변**만 `<Original>` 로 쓴다 | `_drafts/korean-yes-no-ne-negative-questions.mdx` sources |
| **제주어** — 표제어·뜻풀이·용례 (제주학연구센터 「제주어왓」 `jeju.go.kr/jejueo/dictionary/search.htm?qtype=word&query=낱말`, 시험 운영 중 · 강영봉 「제주방언」 `jeju.grandculture.net/jeju/toc/GC00702008`, Chrome 으로 열어야 본문이 나온다) | `_다음편-제주어-Decode.md` |
| **강릉 시내버스** — 노선별 정차 목록·첫차/막차·요금 (`bis.gn.go.kr/search`, Chrome 으로만 열린다. 노선검색 목록의 `.bus-route-nm-box` 를 JS 로 click 하면 정차 목록이 innerText 에 나온다. 300 은 강릉역을 지나지만 **300-1·300-2 는 안 지난다**) · KTX 운임은 `korail.com/ticket/main` 예매 화면에서 읽는다 (Chrome, 다음날 조회) | `src/content/scenes/bts-bus-stop-jumunjin-gangneung-bus-back-to-station.mdx` sources |
| **법령 원문** — `law.go.kr/법령/<법령명>` 은 Chrome 으로만 열린다. `document.getElementById('lawService').contentDocument.body.innerText` 를 5~7초 기다린 뒤 읽는다 (공중화장실법·시행령 확인, 2026-09-06) | `src/content/hangul/korean-toilet-signs-nam-yeo-door-code-paper.mdx` sources |
| **지하철 재승차(화장실) 규칙** — 서울 15분(2023-10-07~, `news.seoul.go.kr/traffic/archives/510086`) · 코레일 구간 15분(2026-06-20~, `korea.kr` newsId=148966539). 둘 다 web_fetch 로 열린다. **1회권 제외** | 같은 기사 sources |
| **부산** — 세 곳 다 web_fetch 로 열린다 (2026-09-09 확인). 명소별 운영시간·요금·교통·여행에티켓은 `visitbusan.net/index.do?menuCd=DOM_000000201001001000&uc_seq=<번호>&lang_cd=ko` (감천문화마을 = 365) · 구청 문화관광은 `saha.go.kr/tour` · 마을 공식은 `gamcheon.or.kr`. ⚠ **게시판과 상품 목록만 Chrome 이 필요하다** (`notice`·`item` 은 JS 렌더). 그리고 감천 공지 33번처럼 **본문이 통째로 그림인 글이 있다** — innerText 가 비면 포기하지 말고 `img[naturalWidth>300]` 을 찾아 브라우저에서 확대해 읽을 것. `lang_cd=en` 주소는 **본문이 비어 나온다 — 주소가 틀린 것이지 영어판이 없는 것이 아니다.** 단정하지 말 것 | `_drafts/gamcheon-culture-village-busan-hours-buses-new-rules.mdx` sources |
| **특별관리지역(관광진흥법 제48조의3)** — ⑥ 차량·관광객 통행 제한 가능, ⑦ **1천만원 이하** 과태료(상한이지 금액이 아니다), ⑧ 안내판 설치 의무. **실제 금액은 지자체 조례에 있다** — 북촌 10만원은 종로구 조례다. 사하구 조례(시행 2025-04-14)에는 **과태료 조항이 아예 없다**(전문 확인). 다른 마을을 볼 때도 모법이 아니라 **그 구의 조례를 열어야** 금액이 나온다 | 같은 기사 sources |
| 상업 링크를 실제로 열어 본 기록 | `data/link-verified.json` |
| 사진을 열어 보고 확인한 기록 | `data/photo-verified.json` |
| 제휴 상품 이름의 단일 출처 | `src/config/products.mjs` |
| 화면 문구 (3개 언어) | `src/config/ui.mjs` |
| 공개 오류 대장 | `src/pages/[lang]/ledger.astro` |

**새 자료를 구하면 위 표에 한 줄 추가한다.** 안 그러면 다음 세션의 내가 또 헤맨다.

## 자주 재발한 사고 — 같은 실수를 세 번 이상 했다

**짐작한 주소로 요청하지 않는다.** 클룩 도시 ID, 트립닷컴 호텔 목록, 서울교통공사
경로 URL — 전부 404 를 보고서야 알았다. 링크 생성기와 API 는 **없는 주소도 받아준다.**

**한 곳을 고치면 옆을 본다.** 영어 원문만 고치고 번역본 둘을 잊는 사고가 반복됐다.
대표사진, CTA 라벨, 코드 블록, 메타 설명이 전부 그랬다.

**게이트에 면제를 적을 때는 대상을 하나도 빠짐없이 열어 본 뒤에 적는다.**
「두 번 확인했다」고 써 놓고 다섯 장 중 넷째를 두 번 다 안 연 적이 있다.

**안 우는 게이트는 없는 게이트보다 나쁘다.** 통과했다고 말하기 때문이다.
게이트를 만들면 **일부러 깨뜨려서 우는지 확인한다.**

**CJK 문장의 `**` 는 강조가 안 될 때가 있다.** 앞뒤가 구두점이면 CommonMark 가
강조로 인정하지 않고 별표가 그대로 화면에 나온다. `<strong>` 을 쓴다.
**영어에서는 거의 재현되지 않는다** — 영어만 보고 있으면 모른다.

가장 잘 걸리는 꼴은 `…は**[링크](…)**に…` 다. 여는 `**` 앞이 문자, 뒤가 `[` 라서
열리지 않는다. 반대로 `**강조**（보충）` 는 닫는 자리라 안전하다.
**그리고 content-quality 게이트는 이 사고의 절반만 보여준다.** 여는 쪽이 실패하면
남은 별표들이 서로 짝을 지어 **엉뚱한 구간이 굵게 나오는데**, 게이트는 짝을 못 찾고
남은 별표만 센다. 그러니 게이트가 「2곳」이라 해도 **그 줄 전체를 다시 읽는다.**

**블록 주석 안에서 별표 뒤에 슬래시를 붙이지 않는다.** 거기서 주석이 닫힌다.

## 글에 관한 것

**본문에 코드 블록을 쓰지 않는다.** 이 사이트에는 코드가 없다.
거기 들어가는 것은 늘 표였다. 게이트가 막는다.

**외국인이 원하는 것은 「나 이런 것도 해봤어」 할 수 있는 경험이다.**
싸고 맛있는 것, 평소 못 보던 풍경, 여행 온 것 같은 설렘.
밤 이야기는 비중을 낮춘다 — **외국인은 낮에 즐기고 밤에는 호텔에 간다.**

**증언과 사실을 구분한다.** 사람에게 들은 것은 「누가 그렇게 말했다」로 쓰고,
n=1·2 를 「한국인은」으로 일반화하지 않는다.

## 검증

```
npm.cmd run og
npm.cmd run verify
```

PowerShell 이 `npm.ps1` 을 막으므로 `npm.cmd` 를 쓴다.
**`Set-ExecutionPolicy` 를 바꾸지 않는다.**

초록이어도 **CSS·도표·번역면은 브라우저로 눈으로 본다.** 게이트가 못 잡는다.

## 내가 할 수 없는 일

깃 푸시, 계정 로그인, 제휴 신청, 검색 콘솔 제출은 **운영자가 한다.**
자격증명을 요구하지 않는다.
