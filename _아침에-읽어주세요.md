# 밤사이 진행한 것 — 2026-08-13

주무시는 동안 **Korea Now 신호 수집기**를 만들었습니다. 여러 후보 중 이걸 고른 이유가 있습니다.

**이것만 시간에 종속된 작업이기 때문입니다.** 트렌드 판정에는 완료된 4주치 신호가 필요한데, 데이터는 소급해서 만들 수 없습니다. 코드는 다음 주에 써도 되지만, 오늘 수집을 시작하지 않으면 첫 판정 시점이 그대로 밀립니다. 사이트가 완성되기 전부터 돌려야 하는 유일한 부품입니다.

---

## 1. 실제 API를 먼저 찔러봤습니다

추측으로 파서를 짜지 않으려고 네이버 DataLab을 실제로 호출했습니다. **설계를 뒤집는 함정이 세 개 나왔습니다.**

### ① ratio는 절대값이 아니라 "요청 안에서의 상대값"입니다

한 요청의 최댓값이 항상 100이 됩니다. 즉 **오늘 수집한 값과 내일 수집한 값을 직접 비교하면 완전히 틀립니다.** 같은 인기도라도 함께 조회한 키워드가 뭐냐에 따라 숫자가 달라집니다.

→ 모든 요청에 **고정 앵커 키워드(`김치`)** 를 끼워 넣고, 앵커 대비 비율만 저장·비교합니다. 앵커는 절대 바꾸면 안 됩니다. 바꾸는 순간 이전 데이터와의 연속성이 끊깁니다.

### ② 마지막 구간은 "진행 중인 주"라 반토막으로 나옵니다

실측값입니다.

```
약과   2026-08-03 → 76.9      2026-08-10 → 26.0   (-66%)
탕후루 2026-08-03 → 79.8      2026-08-10 → 18.6   (-77%)
```

추세가 꺾인 게 아니라 **그 주가 아직 안 끝난 것**뿐입니다. 이걸 그대로 읽으면 멀쩡한 트렌드가 전부 `Cooling Down`으로 오판됩니다. 매주 모든 항목이 폭락하는 대시보드가 나왔을 겁니다.

→ 완료되지 않은 마지막 구간은 버립니다.

### ③ 검색량이 적으면 빈 배열이 옵니다

`두바이스크림`을 조회했더니 `data: []`가 왔습니다. 이건 **"인기가 떨어졌다"가 아니라 "측정이 안 된다"** 입니다. 0으로 처리하면 하락으로 잘못 집계됩니다.

→ `insufficient_volume`으로 따로 표시하고, 모멘텀 계산에서 제외합니다.

---

## 2. 단일 신호 상한 규칙

기획문서 05 §8과 11 §4의 "신호가 하나뿐이면 유행으로 단정하지 않는다"를 **세 겹으로** 강제했습니다.

| 층 | 위치 |
|---|---|
| 코드 | `evaluateStage()` — 독립 소스 2개 미만이면 `emerging`에서 강제로 멈춤 |
| 테스트 | 모멘텀 +300%를 넣어도 `emerging`을 넘지 않는지 검증 |
| DB | `trend_topic.single_source_capped` 제약 — 애플리케이션 버그가 나도 DB가 거부 |

같은 소스에서 나온 신호 2건은 독립 2건으로 세지 않습니다. 이것도 테스트로 못 박아뒀습니다.

---

## 3. 검증 결과

```
정규화 단위 테스트   9개 전부 통과   (실제 API 응답으로 회귀 검증)
파이프라인 드라이런  6개 전부 통과   (배치 1개를 503으로 죽여 실패 격리 확인)
Astro 빌드          11페이지 정상   (회귀 없음)
```

드라이런에서 검색 배치 하나를 의도적으로 503으로 만들었더니, 3회 재시도 후 그 배치만 포기하고 나머지는 정상 수집·저장했습니다. 주 1회만 보시는 운영 조건에서 이게 중요합니다 — 일부가 실패해도 전체가 멈추면 안 됩니다.

---

## 4. 추가된 파일

```
scripts/
├─ collect-trends.mjs          수집기 본체
├─ lib/datalab.mjs             API 클라이언트 (재시도·속도제한·실패격리)
├─ lib/normalize.mjs           함정 3종 방어 + 단계 판정
└─ __tests__/                  단위 테스트 + 드라이런
data/watchlist.json            관측 대상 8개 + 앵커
db/schema-phase2.sql           trend_signal / trend_topic / slang_entry
.github/workflows/collect-trends.yml   매일 04:30 KST 자동 수집
```

정리도 했습니다 — 어제 업로드용으로 만든 `.bat` 두 개를 저장소에서 제거했습니다.

---

## 5. 아침에 하실 일

### (1) 변경분 올리기 — Git Bash에 한 줄

```
cd /c/Users/user/hankukin && git add -A && git commit -m "feat: Korea Now 신호 수집기" && git push
```

### (2) 네이버 개발자 키 발급 — 5분

`developers.naver.com` → 애플리케이션 등록 → **검색 API**와 **데이터랩 API** 선택 → Client ID와 Secret 발급.

받으신 값은 GitHub 저장소 → Settings → Secrets and variables → Actions에서 `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET` 두 개로 등록하시면 됩니다. **저에게 키를 보내지 마세요** — 제가 대신 넣지 않습니다.

이것만 하면 다음 날 새벽부터 자동으로 신호가 쌓이기 시작합니다.

### (3) Cloudflare 계정 — 2분

`dash.cloudflare.com` → Sign up → Google 계정으로. 무료 플랜, 카드 불필요.

### (4) Porkbun 2FA — 5분

아직 안 하셨습니다. 도메인 계정은 모든 자산의 뿌리입니다.

---

## 6. 그다음 제가 할 것

(3)(4)가 되면 Pages 연결 → 첫 배포 → 네임서버 이전 → 도메인 연결까지 이어서 끝냅니다. 사이트가 실제로 인터넷에 뜨는 단계입니다.

그 후 우선순위는 Slang Decoder 엔트리 30개, 자동 발행 파이프라인, 주간 대시보드 순입니다.

---

## 참고 — 제가 못 하는 것들

작업하면서 명확해진 경계입니다. 이건 앞으로도 직접 하셔야 합니다.

- 로그인, 계정 생성, 비밀번호·2FA 설정
- API 키·시크릿 입력
- `git push` (인증이 필요해서)

반대로 코드 작성, 테스트, 검증, 브라우저 조작, 파일 관리는 제가 합니다.
