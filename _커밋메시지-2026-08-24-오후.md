# 커밋 메시지 — 2026-08-24 (월) 오후 실행분

## ⚠ 먼저 읽어 주세요 — 빌드를 못 돌렸습니다

**샌드박스 디스크가 가득 차서 `npx astro build` 를 끝내지 못했습니다.**
`npm ci` 는 통과했는데(EXIT=0), 빌드를 시작한 직후 리눅스 쪽이 통째로 멈췄습니다.
그 뒤로는 `ls` 조차 안 됩니다. 파일 도구(Read/Write/Edit)만 살아 있어서
**원고는 제자리에 뒀지만 「VERIFIED」 줄은 못 씁니다.**

푸시 전에 윈도우에서 이것부터 돌려 주세요.

```
npm run build
```

게이트가 서면 그 파일은 아래 ②번 새 원고입니다. 지우거나 `_drafts/` 로 내리면 됩니다.

**게이트를 하나하나 손으로 대조한 결과는 이렇습니다** (코드를 읽어서 확인, 실행은 못 함):

| 게이트 | 판단 | 근거 |
|---|---|---|
| 공유 카드(og) | **불필요** | 15번 게이트는 og:image 가 `/og/*.png` 일 때만 선다. 이 번역본은 원문 사진을 물려받으므로 해당 없음. ja 번역 13편 중 3편이 카드 없이 통과하고 있는 것과 같은 이유 |
| heroAlt | 통과 | `hero:` 없음 · `heroAlt:` 있음 (content-quality 1-b) |
| stuck.json | 통과 | `guides/hanbok-free-palace-entry-what-counts` 가 이미 등재돼 있고 검사는 슬러그 기준 |
| 번역 낡음 | 통과 | `sourceCheckedAt 2026-08-24` = 원문 `checkedAt 2026-08-24` |
| Klook aid/sponsored | 통과 | 원문과 **같은 URL**, 같은 컴포넌트 경로 |
| seoTitle 60자 | 통과 | 21자 |

확신이 아니라 대조입니다. 빌드가 최종 판정입니다.

---

## ① 원문 두 곳을 고쳤습니다 — 재확인 중에 빠진 것이 나왔습니다

`src/content/guides/hanbok-free-palace-entry-what-counts.mdx`

번역하려고 1차 출처를 다시 열었다가(⑫ "사실은 옮겨 적지 않는다. 다시 연다")
**원문에 없던 것 두 개**를 찾았습니다. 둘 다 독자가 현장에서 헛걸음할 종류입니다.

**(1) 허리치마에는 조건이 붙어 있습니다.**
Q&A 답변이 이렇게 끝납니다 — `단, 저고리가 허리까지 내려와야 합니다.`
원문은 "저고리에 허리치마면 통과" 까지만 적고 이 한 줄을 빠뜨렸습니다.
짧은 저고리 + 허리치마는 사진이 제일 예쁘게 나오는 조합이라 실제로 걸릴 자리입니다.

**(2) 정기휴일이 공휴일과 겹치면 휴일이 옮겨 갑니다.**
관람시간 페이지 원문 — `단, 정기휴일이 공휴일 및 대체공휴일과 겹칠 경우에는 개방하며,
그 다음의 첫 번째 비공휴일이 정기휴일임`.
**월요일이 공휴일이면 창덕궁은 그 월요일에 열고 화요일에 쉽니다.** 그런데 화요일은
원래 경복궁·종묘가 쉬는 날입니다. **그 주만 화요일에 둘 다 닫힙니다.**
휴일을 확인한 사람이 오히려 걸리는 자리라서 넣었습니다.

**쓰다가 제가 한 번 틀렸고 고쳤습니다** (⑤). 허리치마 조건을 넣으면서
"통과하는 것 중 **유일하게** 조건이 붙은 경우" 라고 썼는데, 바로 두 줄 아래 모시옷이
"가이드라인에 준하는 경우" 라는 조건을 이미 달고 있었습니다. 더한 문장이 옆 문장과
어긋난 것이라 영어·일본어 양쪽에서 "이 경우에만 붙는 조건" 으로 고쳤습니다.

`checkedAt` 과 sources 세 건을 전부 **2026-08-24** 로 올렸습니다.
오늘 세 페이지를 실제로 다시 열었습니다 (R705 가이드라인+Q&A · R703 요금 · R702 시간).

---

## ② 새 원고 — 일본어판 (번역 큐 ③번)

`src/content/guides/ja/hanbok-free-palace-entry-what-counts.mdx`

**왜 새 원문이 아니라 번역인가.** ⑪ 의 순서가 그렇습니다 —
① 번역 부채(없음) → ② 90일 경과(없음) → **③ 번역 큐** → ④ 새 원문.
`weekly-plan.mjs` 가 뽑은 ja 큐에 남은 것이 이 한 편뿐이었습니다.

**확인한 것** — 숫자를 옮겨 적지 않고 전부 다시 열었습니다.

- 무료 대상 조항 원문: `공무수행을 위하여 출입하는 자, 한복을 착용한 자`
- 외국인 포함 근거 원문(Q&A): `…내·외국인에 관계없이 무료관람 대상으로 인정합니다`
- 가이드라인 5개 조항 · 불가 사례 · Q&A 9문 전부
- 요금 3,000+3,000+1,000+1,000+1,000 = **9,000원** / 통합관람권 **6,000원**
- 통합관람권 범위: `4대궁 및 종묘(창덕궁 후원 제외)`
- 후원 무료대상: `만6세 이하, 유공자(내국인), 장애인(내국인)` → **한복 없음**
- 매월 마지막 수요일 `문화가 있는 날`(후원 제외)
- 정기휴일 월/화 + 공휴일 이월 규칙
- 덕수궁·창경궁 09:00~21:00 연중 / 경복궁 계절별 17:00~18:30

**`<Original>` 두 곳** 다 오늘 화면에서 그대로 가져온 문장입니다. 지어내지 않았습니다.

**번역하면서 한 번 틀렸고 고쳤습니다** (⑫ "사실이 변하는 것"). 후원 무료대상을
「6歳以下・有功者・障害者（**いずれも**内国人）」라고 썼는데, 원문에서 `내국인` 은
유공자·장애인에만 붙어 있고 만6세 이하에는 안 붙습니다. 괄호 하나가 조건을 넓힐 뻔했습니다.

**사진** — 번역본이라 `hero:` 를 쓰지 않고 `heroAlt:` 만 적었습니다
(`_번역본-대표사진-갈라짐.md` 의 결정). 새로 쓴 사진이 없어 photo-audit 은 돌리지 않았습니다.

**제휴** — 원문과 같은 Klook 한복 대여 링크 하나. `data/link-verified.json` 에
이미 등재된 주소이고, 라벨만 일본어로 썼습니다(⑫). 새로 만들지 않았습니다.

---

## ③ 오늘 새 원문(④)은 쓰지 않았습니다 — 이유

`weekly-plan.mjs` 의 한 줄 요약은 **decode** 라고 했지만, 표를 보면
decode 6 · hangul 6 으로 **동률(둘 다 -4.8)** 입니다. 지시문 ① 의 동률 규칙은
`Scenes → Hangul → Decode` 이므로 정답은 **hangul** 입니다.
**스크립트가 자기 동률 규칙을 안 태우고 정렬 첫 줄을 그대로 뱉는 것 같습니다** —
고칠 거리로 남겨 둡니다. (오늘은 어차피 ③ 번역 큐가 먼저였습니다.)

hangul 축에서 고른 후보는 **지하철 역 표지 읽기**였습니다. stakes 시험은 통과합니다 —
2호선을 반대로 타면 40분, 급행이 내릴 역을 통과하면 되돌아와야 하고, 막차를 놓치면
심야 택시비입니다. 돈·시간 둘 다 걸립니다.

**그런데 1차 출처를 못 열었습니다.** 2호선 내선/외선 표기는 서울교통공사가
행선안내기를 바꾸면서 실제로 달라지고 있는데, **그 변경을 확인해 줄 공식 문서를
찾지 못했습니다.** 나무위키·블로그·언론 요약은 있지만 출처가 아닙니다(③).
지금 상태로 쓰면 "그럴듯한데 틀린" 글이 되고, 그건 사진 오매칭과 같은 종류입니다.

⑧ 에 따라 건너뜁니다. **틀린 글 한 편이 안 쓴 열 편보다 나쁩니다.**
후보는 `_주제은행.md` 에 막힌 지점과 함께 적어 뒀습니다.

---

## 커밋 제안

```
guides: 한복 무료입장 편 재확인 + 일본어판 추가

재확인 중 원문에서 빠진 것 두 개를 찾아 넣었다.
① 허리치마는 통과하되 「단, 저고리가 허리까지 내려와야」 조건이 붙는다.
② 정기휴일이 공휴일과 겹치면 개방하고 휴일이 다음 평일로 옮겨 간다 —
   월요일이 공휴일인 주에는 화요일에 창덕궁과 경복궁이 함께 닫힌다.

일본어판은 ⑪ 의 번역 큐 ③번. 숫자를 옮겨 적지 않고 R705·R703·R702 를
다시 열어 확인했다. 후원 무료대상에서 「내국인」의 범위를 넓게 옮긴 것을
한 번 잡아 고쳤다.

hangul 축(동률 1순위)의 지하철 표지 편은 2호선 내선/외선 표기 변경의
1차 출처를 못 열어 건너뛰었다. 주제은행에 막힌 지점을 남겼다.

걸러낸 사진 없음 — 새로 쓴 사진이 없다.

BUILD NOT RUN - 샌드박스 디스크 소진으로 astro build 미실행.
푸시 전 윈도우에서 npm run build 필요.
```

`VERIFIED` 줄은 **일부러 뺐습니다.** 빌드를 안 돌렸는데 적으면 그 줄이 거짓이 됩니다.
윈도우에서 통과하면 그때 붙여 주세요.
