# 기사 하나가 끝나기까지 — 표준 순서

2026-08-17 운영자 지시로 고정. **이 순서를 바꾸지 않는다.**

> 영어로 먼저 쓰고 → 일본어·중국어를 순차로 올리고 → 제휴를 넣을 수 있으면 넣는다.

---

## 1. 영어로 쓴다 — 여기가 원본이다

번역본은 원본의 사본이지, 별개의 글이 아니다. 영어가 확정되기 전에 번역을 시작하면
영어를 고칠 때 번역이 남는다. **오늘 하루에만 여섯 번 반복한 모양이다.**

발행 전에 통과해야 하는 것:

- **`npm run verify` 하나로 끝낸다** — 빌드 게이트 + check-i18n + link-check.
  2026-08-17 에 앞의 둘만 돌리고 발행했다가 CI 가 세 번째에서 걸렸다.
  로컬 verify 와 CI 가 서로 다른 것을 세고 있었던 것이 원인이다. 이제 같은 것을 센다.
- **상업 링크는 `data/link-verified.json` 에 등재해야 통과한다.**
  열어서 제목·404·추적 파라미터를 확인한 날짜와 함께 적는다.
- 숫자는 **1차 출처**로 확인한다. 못 구하면 안 쓴다 — 추정치를 쓰지 않는다
- 확인 못 한 것은 기사 안에 적고 `data/ledger.json` 의 `withheld` 에 남긴다

## 2. 일본어 → 중국어 순으로 옮긴다

**같은 날 셋을 다 내지 않아도 된다.** 순차로 올린다는 것이 지시의 요점이다.
영어가 하루 이틀 먼저 나가 있어도 괜찮고, 그 사이에 영어가 고쳐지면
번역은 고쳐진 쪽을 옮긴다.

옮길 때 지키는 것:

- `translation.sourceCheckedAt` 이 원본의 `checkedAt` 보다 **빠르면 빌드가 멈춘다** (게이트 10).
  원본이 갱신됐는데 번역이 옛날 사실을 들고 있는 상태를 막는다.
- 원본에 있는 제휴 링크는 번역에도 **전부** 들어가야 한다 (게이트 12).
  번역본이 원본보다 적은 것은 아무 오류도 내지 않으므로 게이트가 대신 센다.
- 도표 안의 글자도 그 언어로 나와야 한다. 컴포넌트에 `lang` 을 넘긴다.
- **한국어가 문장에 섞이지 않았는지 훑는다.** `충전`, `공공자전거` 처럼
  하루에 세 번 나온 적이 있다. 상품명·간판·1차 출처 제목은 일부러 남기는 것이므로 구분한다.

## 3. 제휴는 마지막에, 그리고 넣을 수 있을 때만

**순서가 뒤바뀌면 링크를 정당화하려고 기사를 쓰게 된다.**

넣기 전에 스스로 묻는 것:

- 이 문단에서 독자가 **무엇을 정하는가.** 정하는 게 없으면 링크도 없다.
- 우리가 **평가한 것**인가. 아니라면 특정 상품이 아니라 **검색**을 건다.
  「이 호텔이 좋다」는 못 하는 말이고 「여기서 찾아보라」는 할 수 있는 말이다.
- 그 기사가 무언가를 **비판하고 있는가.** 비판 옆에 대안 상품을 붙이면
  팔려고 깐 것으로 읽힌다. 2026-08-17 에 SRT 기사가 그 경우였고, 넣지 않았다.

기술적으로 지키는 것:

- 주소는 **반드시 열어서 확인한다.** 8월 16일에 생성기가 준 주소로 404 를 만들었고,
  8월 17일에도 짐작한 Trip.com 주소가 404 였다. 숫자 하나가 다르면 404 다.
- Klook 은 `/activity/` 만 추적된다. `/transport/` 는 리디렉션에서 aid 가 사라진다.
- 링크를 **기사에 먼저 넣고**, 그다음에 `merchants.mjs` 에서 스위치를 켠다.
  반대로 하면 푸터가 「제휴 링크가 있습니다」라고 말하는데 그런 링크가 없다.
- 번역본에도 같은 링크가 들어갔는지 확인한다 (게이트 12 가 세지만 눈으로도 본다).

---

## 지금 열려 있는 제휴

| | 상태 |
| --- | --- |
| **Klook** | 가동. `?aid=131289`. 체험·교통·eSIM |
| **Trip.com** | 가동. `Allianceid` + `SID`. 숙박·항공·철도 — **계약 3.2.5(a) 서면 회신 대기 중** |
| Agoda | 심사 중 (최대 1주). 승인되면 Trip.com 옆에 세워 **가격 비교**가 된다 |

Trip.com 은 자연검색 SEO 조항에 대한 회신이 오지 않았다.
**부정 회신이 오면 `merchants.mjs` 한 줄로 다섯 페이지 세 언어가 한 번에 내려간다.**
