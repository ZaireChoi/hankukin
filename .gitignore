node_modules/
dist/
.astro/
.env
.env.*
!.env.example
.DS_Store

# 자동 푸시 로그 (로컬 전용)
.auto-push.log
자동푸시-등록.bat
올리기.bat
사진받기.bat

# 로컬 인증키 (절대 커밋되지 않음)
.env.local
사진받기-촬영지.bat
.photo-fetch.log

# 샌드박스에서 지울 수 없어 옮겨 둔 것 (윈도우에서 폴더째 삭제)
_to_delete/
hankukin-src.tgz

# 예약 작업이 node 를 못 찾을 때 알려 주는 로컬 힌트
scripts/node-path.txt

# 설치·빌드 실행 기록 (로컬 전용, 매번 덮어써진다)
.setup-run.log

# npm ci 가 rename 해 둔 옛 트리 — 윈도우에서 폴더째 지운다
node_modules_old/

# 사진 수집 쿨다운 표시 · 빌드 실패 전체 출력 (로컬 전용)
.photo-fetch.next
.build-fail.log
