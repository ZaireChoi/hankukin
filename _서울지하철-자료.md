# 서울 지하철 자료 — 서울 기사를 쓰기 전에 여기부터 본다

2026-08-23 운영자가 서울교통공사·공공데이터포털에서 받아 주신 자료.
**앞으로 서울 관련 기사에서 역·거리·시간·환승을 말할 때는 지어내지 않고 여기서 꺼낸다.**

## 왜 이 문서가 있나

명동 올리브영 편을 쓸 때 카카오맵 도보 길찾기로 거리를 쟀다. 그건 됐다.
그런데 「어느 동네에 자야 하나」 기사를 쓰려니 **환승 횟수와 소요시간**이 필요했고,
카카오맵 대중교통 API 는 응답하지 않았고, 서울교통공사 사이버스테이션은
내 브라우저에서 결과가 안 떴다. 그래서 운영자에게 스무 건을 손으로 뽑아 달라고 했다.

운영자: **「너가 직접 자료를 구할 방법을 연구해봐. 매번 내가 해줄수 없잖아.」**

맞는 말이었다. 기사 한 편마다 반복될 부탁은 방법이 아니다.

## 가진 것

원본 CSV 는 **EUC-KR(CP949)** 이라 그대로는 못 읽는다 — 「서울역」이 「���￪」로 온다.
`node scripts/import-subway-data.mjs` 가 UTF-8 JSON 으로 바꿔 `data/` 에 넣는다.
자료가 갱신되면 새 CSV 를 `data/raw/` 에 넣고 다시 돌린다.

| 원본 | 만들어지는 것 | 무엇을 할 수 있나 |
| --- | --- | --- |
| 서울시 역사마스터 정보 | `data/subway-stations.json` | 역 이름·호선·**위경도**. 좌표가 있으니 두 역 사이 직선거리를 직접 계산할 수 있다 |
| 서울교통공사_역간거리 | `data/subway-segments.json` | 구간별 **소요시간(초)** 과 거리(km), 노선별 누계 |
| 서울교통공사_환승역거리 소요시간 정보 | `data/subway-transfers.json` | 환승역의 **환승 거리와 걷는 시간** |

이 셋을 합치면 **환승 횟수와 총 소요시간을 스스로 계산할 수 있다.**
구간 시간을 더하고, 환승할 때마다 환승 걷는 시간을 얹으면 된다.

## 반드시 지킬 것

**① 출처를 적는다.** 공공데이터포털 자료다. 기사에 숫자를 쓸 때
`서울교통공사`를 출처로 표기한다. TourAPI 사진에 한국관광공사를 적는 것과 같은 규율이다.

**② 환승 시간은 실측이 아니다.** 서울교통공사가 **보행속도 1.2 m/s** 로 계산한 값이다.
기사에 쓸 때 그 사실을 밝힌다. 「7분 걸린다」가 아니라
「공사 기준(초속 1.2 m)으로 7분」이라고 쓴다. 짐 든 사람은 더 걸린다.

**③ 노선이 다 있는 게 아니다.** 서울교통공사 관할은 **1~8호선**이다.
우이신설선·공항철도·신분당선·경의중앙선·수인분당선은 빠져 있을 수 있다.
**북한산우이가 안 나올 가능성이 크다** — 하필 등산 기사의 핵심 자리다.
없으면 없다고 쓰고, 다른 출처를 찾는다. **있는 것처럼 계산하지 않는다.**

## 아직 못 한 것

**4개 언어 노선도 PDF** (국문·영문·일본어·중문 간체, 2025-09-29).
이게 자료 중 값어치가 제일 클 수 있다 — **서울교통공사가 정한 역 이름의 공식 영어·일본어·중국어 표기**다.
번역본 기사에서 역 이름을 우리가 지어내지 않아도 된다.

지금은 못 읽었다. PDF 를 그림으로 펼치는 도구가 샌드박스에 있는데
그 샌드박스가 디스크가 차서 죽어 있다(내가 채웠다). 살아나면 뽑아서
`data/subway-station-names.json` 으로 만든다. 그때까지 역 이름 로마자는
[국립국어원 로마자 표기](https://www.korean.go.kr/) 규칙과 기존 기사 표기를 따른다.

## 앞으로 이걸 쓸 기사

- **서울 어디에 자야 하나** (허브) — 숙소 후보 4곳 × 목적 5개의 환승·시간 표. 이 자료가 없으면 못 쓴다
- 등산 편 — 우이신설선 환승역이 셋뿐이라는 것이 요점
- 공항 이동 편 — 이미 AREX 요금은 확인해 두었고, 여기에 환승이 붙는다
- 번역본 전체 — 역 이름 표기(PDF 해결 후)

## 관련 파일

- `scripts/import-subway-data.mjs` — CSV → JSON 변환. 한 번만 돌리면 된다
- `scripts/probe-subway-path.mjs` — 공공데이터포털 최단경로 API 탐침.
  CSV 로 직접 계산할 수 있게 됐으니 **급하지 않다.** 다만 「공사가 계산한 경로」와
  「우리가 계산한 경로」를 대조하는 용도로는 여전히 값어치가 있다
- `data/raw/` — 받은 원본 CSV. 손대지 않는다
