# 커밋 메시지 — 2026-08-21 오후

**발행 1편.** 오전에 멈춰 있던 원고를 검증해서 내보냈습니다.

```
feat(hangul): 도로명주소 편 발행 — 오전에 못 돌린 게이트를 돌렸다

VERIFIED - astro build 통과 (117 pages)
```

오늘 아침 실행분은 글을 다 써 놓고 **리눅스 샌드박스 디스크가 차서** `npx astro build` 를
한 번도 못 돌렸습니다. 그래서 `_drafts/` 에 두고 0편으로 끝냈습니다
(`_커밋메시지-2026-08-21.md` §8). 오후 실행분에서 샌드박스가 살아났고,
그 파일 §5 의 순서를 그대로 밟았습니다. **새로 쓴 글이 아니라 검증하고 내보낸 글입니다.**

---

## 1. 무엇이 나갔나

```
src/content/hangul/korean-address-road-name-building-number.mdx
src/components/ChartAddressDistance.astro
src/integrations/content-quality.mjs      HERO_EXEMPT 한 줄
data/stuck.json                           「무엇이 막혔나요」 한 항목
public/og/hangul__korean-address-road-name-building-number__en.png
```

**Hangul / How to Read a Korean Address: Every Number Is a Distance in Metres**
seriesPart 6 · 2,078단어 · 고지 160단어(**7.7%**, 상한 10%) · 내부 링크 4개 ·
원문 병기 3개 · 도표 1개 · 대표사진 없음(면제)

자동 푸시가 14:10 에 5개 파일을 `auto(content): 4 files - verified` 로 올렸습니다.

---

## 2. 축 — 아침 판단 그대로입니다

| 축 | 발행 전 | 발행 후 | 목표 | 차이 |
|---|---:|---:|---:|---:|
| Scenes | 12 | 12 | 30% | −3 |
| **Hangul** | **5** | **6** | **20%** | −5 → **−4** |
| Decode | 5 | 5 | 20% | −5 |
| Now | 18 | 18 | 20% | +8 |
| Guides | 10 | 10 | 10% | +5 |

Hangul·Decode 가 −5 동률이었고 동률 순서(Scenes → Hangul → Decode)에 따라 Hangul 이었습니다.
**다음 실행분은 Decode 가 최미달(−5)입니다.** `_다음편-제주어-Decode.md` 에 준비된 것이 있습니다.

stakes 시험은 아침 §3 에서 세 줄 다 답이 나왔습니다 — 주소를 손에 들고 택시에 탄 사람이,
지도가 도로만 찍어 주는 자리에서, 시간과 돈을 잃습니다.

---

## 3. 오후에 실제로 한 일 — 도표를 눈으로 보고 두 군데 고쳤다

**게이트 넷은 처음부터 다 통과했습니다.** 그리고 도표는 망가져 있었습니다.
이게 ⑥ 이 「빌드가 통과해도 눈으로 확인하라」고 적어 둔 이유입니다.

`dist` 에서 SVG 를 꺼내 PNG 로 렌더해서 열어 봤고, 실제로 두 군데가 틀려 있었습니다.

| 무엇 | 왜 생겼나 | 고침 |
|---|---|---|
| `1` 과 `2` 가 **라벨 위에 겹쳐** 찍혔다 | 라벨 `ODD — your left` 끝이 x≈180 인데 첫 눈금이 x=124 | `T0` 를 `RX+46` → `RX+130` 으로 |
| 계산 상자 둘째 줄이 **화면 밖으로 잘렸다** | 103자가 상자(x 78~682)는커녕 viewBox(760)를 넘었다. `…152 would be about` 에서 끊겨 있었다 | `and 152 would be about 1.5 km` 를 뺐다 |

라벨을 피해 눈금을 오른쪽으로 미니 다섯째 칸(9/10, 80 m)이 `and so on` 과 부딪혀서
**네 칸으로 줄였습니다.** 0/20/40/60 m 로도 규칙은 다 보입니다.

**152 를 도표에서 뺀 것은 손실이 아닙니다.** 본문 첫 문단과 「Two documents, one multiplier」
절에 이미 두 번 있습니다 — 아침 §7 이 「강남대로 699 가 세 번 나온다」고 잡았던 것과 같은 종류입니다.

**뺀 뒤에 그 옆을 다시 읽었습니다 (⑤).** `aria-label` 이 152 도 80 m 도 언급하지 않아
그대로 맞습니다. 아침에 정확히 이 자리에서 어긋났던 적이 있어서(§7-2) 먼저 확인했습니다.
본문에도 도표의 눈금 개수를 가리키는 문장은 없습니다 (`diagram|chart|shown above|80 m` 로 훑었습니다).

수정 뒤 다시 렌더해서 **한 번 더 열어 봤고**, 겹침도 잘림도 없습니다.
공유 카드도 열어서 제목 세 줄이 안 잘리는 것을 확인했습니다.

> 렌더 화면에서 한글(강남대로·대정로23번길·길·번)이 빈칸으로 보입니다.
> **이건 도표 문제가 아니라 샌드박스 ImageMagick 에 CJK 폴백 폰트가 없어서입니다.**
> 추측하지 않고 `dist` 의 HTML 을 직접 grep 해서 네 문자열이 다 살아 있는 것을 확인했습니다.

---

## 4. 확인한 것과 확인하지 못한 것

**사실 확인은 아침 실행분이 1차 출처 원문을 열어서 했습니다.**
`_커밋메시지-2026-08-21.md` §4 의 표 14줄이 그 기록입니다 — 법제처 시행령·시행규칙·법률과
행안부 주소정보누리집. 오후에는 **그 확인을 다시 하지 않았고**, 새 사실도 더하지 않았습니다.
오후에 한 것은 검증 실행과 도표 수정뿐입니다.

**확인하지 못한 것은 본문 맺음말에 그대로 있습니다** (160단어, 7.7%):
10m 상수는 조문에 없는 유도값이라는 것, 홀수가 북·서쪽이라는 것은 우리 기하라는 것,
현장에서 도로·주소·표지판을 한 건도 안 봤다는 것, 119 가 국가지점번호를 어떻게 처리하는지
시험하지 않았다는 것. 국가지점번호판의 색·모양은 시행규칙 별표를 못 열어 **쓰지 않았습니다.**

**걸러낸 사진 — 없습니다. 애초에 쓰지 않았습니다.**
도로명판·건물번호판 사진이 재고 242장에 없고, 있어도 판 하나를 크게 찍으면
이 기사가 가르치는 **번호와 거리의 관계**가 안 보입니다. 눈금자 도표가 사진보다 정확합니다 —
로마자편·방(房)편·병원 간판편과 같은 판단이고, 그 사유를 `HERO_EXEMPT` 에 적었습니다.

---

## 5. 검증 기록

⑥-A 를 지켰습니다. **실제 폴더에 `npm`·`npx` 를 어떤 형태로도 돌리지 않았습니다.**
`/tmp/hk` 복사본을 만들어 거기서만 `npm ci` 와 `npm run verify` 를 돌렸습니다.
⑦ 대로 샌드박스에서 `git commit`·`git add` 도 하지 않았습니다 — 자동 푸시가 올렸습니다.

```
no-shared-images     사진 113장 · 중복 없음                          ✓
no-duplicate-files   사진 319장 · 내용 중복 없음                     ✓
photo-sanity         사진 장소 54곳 · 이름 검사 · 확인 대장 최신     ✓
content-quality      118쪽 · 공유 카드 전부 존재                     ✓
check-i18n           117쪽 · canonical · hreflang · 제휴 추적        ✓
link-check           기사 72편 · 주소 177개                          ✓
```

새 기사에 붙은 경고는 **0건**입니다.
남은 경고 2건은 성수동 편의 길찾기 링크(`호선`·`수인분당선`)로, 오늘 것이 아니고 발행을 막지 않습니다.

```
VERIFIED - astro build 통과 (117 pages)
```

---

## 6. 운영자에게 — 두 가지

**① `_drafts/` 에 사본 두 개가 남아 있습니다.**

```
_drafts/korean-address-road-name-building-number.mdx
_drafts/ChartAddressDistance.astro
```

`src/` 로 복사는 됐는데 **샌드박스에 삭제 권한이 없어서**(`Operation not permitted`)
원본을 못 지웠습니다. 빌드에는 영향이 없습니다 — `_drafts/` 는 컬렉션 밖입니다.
다만 같은 글이 두 곳에 있는 상태라 윈도우에서 지워 주시면 됩니다.

**② 도표 수정분이 아직 커밋 대기 중입니다.**
자동 푸시가 14:10 에 올린 판은 **도표를 고치기 직전 판**입니다.
`src/components/ChartAddressDistance.astro` 한 파일이 워킹트리에 남아 있고,
다음 10분 주기에 검증되어 올라갑니다. 그 전에 사이트를 보시면 도표 숫자가 라벨에 겹쳐 보입니다.

`.git` 잠금 파일은 오늘도 만들지 않았습니다.

---

## 7. 다음 실행분

**Decode 가 −5 로 최미달입니다.** `_다음편-제주어-Decode.md` 에 준비된 것이 있습니다.
다만 ①-0 대로 **stakes 를 먼저** 보십시오 — 제주어는 「알아두면 좋은」 쪽으로 기울 위험이
있는 주제입니다. 세 줄에 답이 안 나오면 계획표 §5 B그룹(서울 밖)으로 넘어가는 편이 낫습니다.
