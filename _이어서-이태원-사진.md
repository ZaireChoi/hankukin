# 이어서 할 일 — 이태원 편 사진 (2026-08-20)

세션 인계 메모. **샌드박스 셸이 죽어서 `npm run verify` 를 한 번도 못 돌렸다.**
그 상태로 두 번 올렸고 두 번 다 게이트에서 섰다. 새 세션의 첫 일은 **검증**이다.

## 지금 상태 — 파일은 다 고쳐져 있다. 커밋만 안 됐거나, 커밋됐는데 빌드가 섰다

사이트는 아직 사진이 없다 (og:image 가 여전히 공유 카드).

### 손댄 파일 6개

| 파일 | 무엇을 |
| --- | --- |
| `src/content/scenes/itaewon-….mdx` | hero = itaewon-4 · 본문 Figure = itaewon-2 |
| `src/content/scenes/ja/itaewon-….mdx` | 같음 (경로 `../../../`) |
| `src/content/scenes/zh-hans/itaewon-….mdx` | 같음 |
| `src/integrations/no-shared-images.mjs` | **게이트 버그 수정** — 아래 참조 |
| `src/integrations/content-quality.mjs` | HERO_EXEMPT 에서 이태원 항목 삭제 + 왜 틀렸는지 주석 |
| `data/photo-verified.json` | `itaewon` 항목 추가 (photo-sanity 게이트가 요구) |

## 첫 번째로 섰던 것 — `no-shared-images` (고쳤고, 통과 확인됨)

같은 사진이 세 기사에 있다며 **같은 이름을 세 번** 찍었다. 영어·일본어·중국어판이었다.
기사를 `basename` 으로만 식별해서 언어 폴더가 사라진 것이다.

이 게이트가 막으려던 것은 *독자가 다른 글에서 같은 풍경을 또 보는 일*이고,
번역판은 다른 글이 아니다. 기사 신원을 「구획 + 슬러그」로 잡고 언어를 빼도록 고쳤다.

**사진 있는 기사를 번역한 것이 이번이 처음이라 여태 안 드러났다.**
빌드 로그에서 `사진 113장 · 중복 없음` 으로 통과 확인.

## 두 번째로 섰던 것 — `photo-sanity` (고쳤으나 미검증)

`data/photo-verified.json` 에 `itaewon` 기록이 없었다. 그래서 다섯 장을 전부 열었고,
**내가 오늘 낮에 적어 둔 것이 틀렸다는 것이 드러났다.**

> "이태원 사진 5장이 전부 옷가게 실내다. **두 번 확인했다.**"

**itaewon-4 는 실외 거리 사진이다.** 해질녘 골목, 「이태원시장 / ITAEWON MARKET」
파란 세로 간판이 화면 가운데. 두 번 확인했다면서 두 번 다 넷째 장을 안 열었다는 뜻이다.

→ 대표사진을 itaewon-4 로 바꾸고, itaewon-2(복도를 혼자 걸어가는 사람)를 본문으로 내렸다.

## 새 세션에서 할 것 — 순서대로

1. `npm run verify` (= og → build → check-i18n → link-check --offline)
   **여기서 또 다른 게이트가 걸릴 수 있다. 셋째·넷째가 없다는 보장이 없다.**
2. `git archive HEAD` 를 빈 디렉터리에 풀어서 거기서 다시 검증 (작업 사본 말고)
3. 통과하면 운영자에게 커밋 요청
4. 배포 뒤 `/en|ja|zh-hans/scenes/itaewon-…/` 를 실제로 열어서 **사진이 보이는지** 확인
   — og:image 가 공유 카드가 아니라 itaewon-4 를 가리켜야 맞다

## 남은 것

- `public/og/scenes__itaewon-…__{en,ja,zh-hans}.png` 3장이 이제 주인이 없다.
  대표사진이 생기면 그 사진이 og:image 가 되므로 `make-og.mjs` 가 다음 실행 때 비운다.
  `npm run verify` 안에 `npm run og` 가 들어 있으니 1번에서 같이 정리된다.

## 기록 — 오늘 배운 것

**면제 사유에 확신을 적기 전에, 대상을 하나도 빠짐없이 열었는지부터 본다.**
「두 번 확인했다」가 제일 나빴다. 확인 횟수를 적으면 읽는 사람에게 신뢰가 생기는데
정작 다섯 장 중 넷째를 두 번 다 안 봤다. 면제는 게이트를 끄는 일이다.
