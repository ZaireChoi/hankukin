# 글 하나 올리는 법 — 도움 없이

이 문서는 **Claude 없이 혼자 기사를 발행할 때** 보는 것입니다.
게이트가 스무 개쯤 있는데, 그중 무엇이 왜 우는지를 미리 알면 대부분은 한 번에 통과합니다.

## 0. 어디에 만드나

| 축 | 폴더 | 무엇 |
| --- | --- | --- |
| Scenes | `src/content/scenes/` | 가서 볼 곳 · K-culture |
| Now | `src/content/now/` | 값·시간·규정. 바뀌는 것 |
| Guides | `src/content/guides/` | 궁궐·규정 같은 방문 실무 |
| Decode | `src/content/decode/` | 자막이 빠뜨리는 말 |
| Hangul | `src/content/hangul/` | 간판 읽는 법 |

파일명이 그대로 주소가 됩니다. `korea-atm-foreign-card-cash.mdx` → `/en/now/korea-atm-foreign-card-cash/`
**영어 소문자와 하이픈만.** 검색어가 앞에 오게 짓습니다.

번역본은 `now/ja/같은이름.mdx` — 언어 폴더만 끼웁니다. 파일명은 원문과 같아야 합니다.

## 1. 최소 frontmatter

이대로 복사해서 채우면 통과합니다.

```
---
title: "화면에 걸리는 제목. 길어도 좋다"
seoTitle: "검색 결과용 짧은 제목 (60자 이내)"
summary: "20~400자. 검색 결과의 설명이 되고, 목록에도 쓰인다."
lang: en
region: "Seoul"
publishedAt: 2026-08-27
checkedAt: 2026-08-27
riskGrade: green
contentScore: 80
visitKorea: []
sources:
  - title: "출처 이름 — 기관명"
    url: "https://..."
    type: public_institution
    checkedAt: 2026-08-27
---
```

**고정값처럼 쓰는 것들**

- `riskGrade` — `green` 또는 `yellow`. 사실이 흔들릴 여지가 있으면 yellow
- `contentScore` — 65~100. 65 미만은 빌드가 선다
- `sources` — **최소 하나. 비우면 빌드가 선다**
- `type` — `public_institution` · `merchant_official` · `reliable_media` · `aggregate_data` · `official_production` · `official_interview` · `official_social` · `editorial` · `community_tally`

## 2. 게이트가 우는 자리 — 자주 걸리는 순서대로

**대표사진이 없습니다**
사진을 넣거나, 못 넣는 **이유**를 `src/integrations/content-quality.mjs` 의 `HERO_EXEMPT` 에 적습니다.
이유를 적기 귀찮으면 사진을 넣게 되는데, 그게 이 게이트의 목적입니다.

**캡션 없는 사진 N장**
`<Figure>` 에는 `caption=` 이 반드시 있어야 합니다. 캡션은 사진이 말하지 않는 것을 말하는 자리입니다.

**코드 블록 N개**
이 사이트에는 코드가 없습니다. 백틱 세 개로 감싼 것이 있으면 **표로 바꿉니다.**
거기 들어가려던 것은 늘 표였습니다.

**「무엇이 막혔나요」 색인에 없습니다**
`data/stuck.json` 에 한 줄 답을 넣거나, `stuckExempt` 에 **이유를 적고** 뺍니다.
막힌 순간이 아닌 글(계절·소개)이면 빼는 게 맞습니다.

**제목이 60자를 넘습니다**
`seoTitle` 을 짧게 씁니다. `title` 은 길어도 됩니다.

**다른 기사로 가는 링크가 없습니다** (경고)
본문에 `](/en/…)` 형태의 내부 링크를 하나 이상 넣습니다.
번역본은 `](/ja/…)` — **자기 언어로** 링크해야 합니다.

**상업 링크가 등록되지 않았습니다**
제휴 링크는 `data/link-verified.json` 에 **직접 열어 본 기록**이 있어야 합니다.
그리고 라벨은 `src/config/products.mjs` 의 것과 글자까지 같아야 합니다.

**번역본에는 hero 를 적지 않습니다**
번역본은 사진을 원문에서 물려받습니다. `heroAlt` 만 그 언어로 적습니다.

**화면에 마크다운 기호(**)가 그대로 나옵니다**
CJK 문장에서 자주 납니다. `<strong>…</strong>` 로 바꿉니다. 자세한 것은 `CLAUDE.md`.

## 3. 쓰다 마는 경우

frontmatter 에 `draft: true` 를 넣으면 **게이트를 건너뜁니다.** 화면에도 안 나갑니다.
커밋해 두고 며칠 뒤에 이어 써도 빌드가 안 섭니다.

`draft: false` 로 바꾸는 순간 전부 검사합니다. **유예이지 면제가 아닙니다.**

## 4. 발행

```
cd /d C:\Users\user\hankukin && npm.cmd run og && npm.cmd run verify
```

PowerShell 이 `npm.ps1` 을 막으므로 `npm.cmd` 를 씁니다.
`&&` 로 묶으면 앞이 실패했을 때 뒤가 안 돕니다 — 그게 맞습니다.

초록이 나와도 **브라우저로 한 번 봅니다.** 게이트는 CSS·도표·번역면을 못 잡습니다.

그다음 커밋·푸시하면 Cloudflare 가 몇 분 안에 배포합니다.

## 5. 막히면

게이트 메시지는 **무엇을 어떻게 고치라는 것까지** 적혀 있습니다. 그대로 따르면 됩니다.
메시지가 불친절하면 그건 게이트의 잘못이니, 고칠 때 메시지도 같이 고쳐 두십시오.

그리고 이 저장소의 규율 하나만 기억하면 나머지는 따라옵니다.

> **숫자와 사실은 1차 출처를 내 눈으로 열어 본 것만 쓴다.
> 못 구하면 안 쓴다. 「없다」고 적는다.**
