# 본문 안 제휴 링크 — 게이트 먼저, 컴포넌트 나중 (2026-08-20)

> 운영자: *"가고싶은데. trip.com 제휴가 없네"* → *"너가 판단하여 나은방향으로 진행해"*

**판단: B(본문 인라인)로 갑니다.** A(그대로)는 문제를 안 고치고,
C(CTA 위치 이동)는 전 기사에 영향을 주는 무딘 수단입니다.

다만 B 의 위험은 실재합니다 — **상업 링크 경로가 둘이 됩니다.**
그래서 순서를 뒤집었습니다.

```
① 게이트   ← 오늘 만들었다 (경고 단계)
② 눈으로 확인 — 기존 링크가 전부 통과하는가
③ 게이트를 hard 로 (throw 한 줄 주석 해제)
④ 그 다음에 <Affiliate/> 컴포넌트
⑤ 제주 편 운전 절에 인라인으로 건다
```

**컴포넌트를 먼저 만들지 않은 이유.** 검사 없는 두 번째 경로가 하루라도 존재하면
이 저장소에서는 늘 사고로 끝났습니다(eSIM 편 링크 증발, CTA 꼬리말 세 번의 거짓 고지,
`/transport/` 에서 aid 소실). 하루도 두지 않겠습니다.

---

## ① 만든 것 — 열여섯 번째 게이트

`src/integrations/content-quality.mjs` 의 `astro:build:done` 안.

**소스를 안 봅니다. 최종 HTML 의 `<a>` 를 전부 봅니다.**
그래서 frontmatter 로 넣든 본문 컴포넌트로 넣든 똑같이 걸리고,
나중에 경로를 하나 더 만들어도 검사가 저절로 따라옵니다.

| 검사 | 안 지키면 벌어지는 일 |
|---|---|
| `data/link-verified.json` 에 등재된 주소인가 | 열어 보지 않은 링크가 발행된다 (404·엉뚱한 상품) |
| 추적 태그가 붙어 나갔는가 | **페이지는 멀쩡하고 수수료만 0 이 된다** |
| `rel="sponsored"` 가 붙었는가 | FTC 고지 누락. 전환이 아니라 법의 문제 |

## ⚠ 오늘 돌려 보지 못했습니다 — 그래서 경고 단계입니다

이 게이트를 쓴 직후 **샌드박스 디스크가 가득 차서 셸이 아예 뜨지 않았습니다.**
`npx astro build` 를 **한 번도** 못 돌렸습니다.

검증 못 한 검사를 hard gate 로 넣으면 오탐 하나에
**윈도우 자동 푸시의 빌드가 실패하고 발행이 통째로 멈춥니다.**
2026-08-19 에 정확히 그 일이 있었고, 그날 쓴 기사는 한 번도 못 올라갔습니다.

그래서 두 겹으로 막아 두었습니다.

```js
logger.warn(...)                 // throw 가 아니다
// throw new Error(...)          // ③에서 이 줄만 살린다
...
} catch (e) { logger.warn(...) } // 검사 자체가 터져도 빌드는 산다
```

**어떤 경우에도 이 게이트가 발행을 멈출 수 없습니다.** 그게 오늘의 설계 목표였습니다.

## ② 다음에 할 일 — 5분이면 됩니다

디스크가 풀리면:

```bash
cd /tmp/사본 && npm run verify
```

로그에서 **「상업 링크 점검(경고 단계)」** 줄을 봅니다.

- 경고가 **0 건**이면 → `throw` 주석을 풀고 hard gate 로 올립니다
- 경고가 뜨면 → **그게 진짜 문제일 수 있습니다.** 지금 발행 중인 링크 중
  등재 안 됐거나 태그가 빠진 것이 있다는 뜻입니다. 하나씩 확인합니다.
  (특히 일본어·중국어판 페이지 — 번역본에도 같은 링크가 들어가야 한다는 게이트가 따로 있습니다)

## ④ 컴포넌트 설계 (게이트가 hard 가 된 뒤)

```astro
---
// src/components/Affiliate.astro
import { product } from '../config/products.mjs';
import { decorate, isAffiliate } from '../config/merchants.mjs';
const { keyword, lang = 'en' } = Astro.props;
const item = product(keyword, lang);          // ← URL 을 직접 받지 않는다
const href = decorate(item.url, item.merchant);
---
<a href={href} rel={isAffiliate(item.merchant) ? 'sponsored noopener' : 'noopener'} target="_blank">…</a>
```

**핵심은 `keyword` 만 받는다는 것입니다.** URL 을 prop 으로 받게 만들면
products.mjs 를 우회할 수 있고, 그 순간 단일 출처가 깨집니다.
없는 키를 넘기면 `product()` 가 던집니다 — 조용히 빈 링크가 되는 것보다 낫습니다.

⚠ 상단 고지(`AffiliateTopNote`)는 지금 **frontmatter 배열만 셉니다.**
본문에만 링크가 있고 frontmatter 에 없으면 고지가 안 뜹니다.
→ 컴포넌트를 만들 때 이것도 같이 고쳐야 합니다. **옆을 보는 일이 이것입니다.**

## ⑤ 제주 편에 걸 자리

운전 절 끝 — 「국제면허가 되면 직접 몰아라, 안 되면 이 방법이 있다」 바로 그 문단.
독자가 그 문단에서 정하는 것이 정확히 그것이기 때문입니다.

오늘은 새 경로 없이 **아래에 무엇이 있는지 한 줄로 알리는 것**까지만 했습니다.
