# 이어서 할 것 — 2026-08-13 저장 시점

## 지금 살아있는 것

| | 상태 |
|---|---|
| 사이트 | **https://www.hankuk-in.com** (HTTPS, 자동 배포) |
| 도메인 | hankuk-in.com — Porkbun 2년, DNS는 Cloudflare |
| 이메일 | hello@hankuk-in.com → suyol1974@gmail.com 전달 |
| 발행 콘텐츠 | Scene 1편(도깨비 주문진), Culture 1편(동궁) |
| 자동 수집 | 신작 레이더 — 화·금 05:00 KST |
| Gmail 라벨 | HANKUKIN / 오류신고 / 제휴문의 |

## 등록된 API 키

| Secret | 상태 |
|---|---|
| `YOUTUBE_API_KEY` | 작동 확인 |
| `DATA_GO_KR_KEY` | 작동 확인 (한국관광공사) |
| `NAVER_CLIENT_ID` / `SECRET` | **미작동** — 제휴 심사 중 (1~2주) |
| `ANTHROPIC_API_KEY` | 미등록 — 초안 생성(2단계)에 필요 |

---

## 마지막 검증에서 드러난 세 가지

TourAPI 실호출 결과다. 코드에 아직 반영하지 않았다.

### ① 검색 실패 — `주문진해수욕장` 0건

실재하는 유명 해수욕장인데 키워드 검색으로 안 잡힌다.
표기 차이(`주문진 해수욕장`, `주문진해변`) 또는 등재명 불일치로 추정.

**보완**: `areaBasedList2` 지역기반 조회를 병행하고, 키워드 변형(띄어쓰기 유무)을 순차 시도.

### ② 동명이인 — `경복궁` 검색에 울산 업소가 섞임

```
· 경복궁 — 서울특별시 종로구 사직로 161   ← 궁궐
· 경복궁 — 울산광역시 남구 산업로 595     ← 동명 업소
```

자동으로 첫 결과를 채택하면 엉뚱한 좌표가 들어간다.

**보완**: 기대 지역(시·도)과 대조하고, 후보가 2건 이상이면 자동 채택을 금지하고 브리프에 선택지로 남긴다.

### ③ 촬영지 언급 판정은 작동함

경복궁·위양지 모두 소개글에 `촬영/드라마/영화` 언급이 **없음**으로 정확히 판정됐다.

이로써 **위양지 건은 종결**이다. 블로거가 동궁 촬영지라 주장했으나
한국관광공사 소개글에도 언급이 없다. 게이트는 계속 닫아둔다.

---

## 추가 — 우리가 지우고 있던 정보

운영자 질문: *"작품명과 장소 이름도 조사해서 적어야 하는 것 아니냐."*

맞는 지적이었고, 확인해 보니 **조사할 필요조차 없었다.**
한국관광공사 소개글은 작품명을 이렇게 적는다.

```
주문진 방파제는 드라마 <도깨비> 촬영지로 유명해졌다.
```

그런데 우리 `stripTags` 가 `/<[^>]+>/g` 로 꺾쇠 안을 전부 지웠다.
`<도깨비>` 를 HTML 태그로 오인한 것이다. 저장된 소개글에는 공백 두 칸만 남아 있었다.

**찾으려던 정보를 우리 손으로 삭제하고 있었다.**

고친 것:

| | |
|---|---|
| `stripTags` | 실제 HTML 태그 이름 화이트리스트로 좁힘 (`<Winter Sonata>` 도 보존) |
| `extractWorkTitles` | 괄호류 9종에서 작품명 추출, 촬영 문맥이 근처에 있을 때만 채택 |
| `discover-locations.mjs` | 촬영지 키워드로 훑어 **작품 → 장소** 색인 자동 생성 |
| `enrich-locations.mjs` | `REFRESH_OVERVIEW=1` 로 기존 소개글 재수집 |

이제 장소 발굴이 **사람의 기억이 아니라 공공기관 데이터**에서 나온다.
기억으로 적는 것은, 우리가 반려한 블로거의 목격담과 근거의 등급이 같다.

**발굴 결과는 후보이지 승인이 아니다.** 게이트는 그대로 작동한다.
`data/filming-index.json` 에서 `locations.json` 으로 옮길 때
작품 동일성(동명 작품·리메이크·영화판 구분)은 여전히 사람이 확인해야 한다.

---

## 다음 작업 순서

**1. `discover-locations` 워크플로 첫 실행** → 색인 확인 → 상위 작품을 `locations.json` 으로 이관

**2. 문화·역사 기사 계속 쓰기** — 지금 가장 막힘이 없는 축
승인도 촬영지 확인도 필요 없다. 90일 목표 15편 중 1편 완료.
후보: 한복과 궁궐 무료입장 / 서연과 조선의 교육 / 궁궐 건축의 방위 / K-pop 팬 문화의 역사

**3. 레이더 가중치 재검토** — 기사 몇 편 써본 뒤에
현재 신작성 50% · 촬영지 35%. `The East Palace`가 촬영지 적합도 만점인데
4주차라 밀린다. 실제로 무엇이 좋은 후보였는지 데이터가 쌓인 뒤 조정.

**4. 네이버 승인 후** — 트렌드 신호 보강 (Korea Now 축 개시)

**5. Claude API 키 등록 후** — 초안 생성 2단계
단, 게이트를 통과한 건에 대해서만. 월 1~3만원 예상.

---

## 미뤄둔 것

- 문의 폼 (지금은 이메일만) → 오류신고 2건/48시간 자동 비공개 규칙과 연결 필요
- 이메일 자동응답 → 운영자가 Gmail 예약작업으로 별도 진행 예정
- 방어 도메인 (gethankukin.com 등) → 첫 수익 발생 시 재검토
- AdSense 신청 → 원문 25~30편 확보 후 (D+45 목표)
- 사업자등록 → 제휴 정산 개시 전

---

## 오늘 얻은 원칙

오늘 발견하고 고친 버그는 **전부 같은 종류**였다. 실패하지 않고 그럴듯하게 틀리는 것.

- 빈 파일 커밋하고 초록불
- 한국 차트를 글로벌이라고 표시
- 7주차 작품을 신규 진입으로 판정
- 미국 다큐를 1순위 후보로 추천
- 한국어 제목을 매칭에서 통째로 배제
- 인도 시트콤을 한국 드라마 화제성 근거로 집계
- 루트 페이지에 "Redirecting..." 2초 노출

빌드 통과, 테스트 통과, 초록불 — 어느 것도 '작동한다'의 증거가 아니었다.
**실제 데이터를 넣고 산출물을 직접 열어봤을 때만** 드러났다.

그리고 마지막 하나는 운영자가 발견했다. 이상하면 알려주는 게 제일 빠르다.
